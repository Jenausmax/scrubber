# scrubber

## What This Is

Кросс-платформенное десктоп-приложение (Windows, Linux, macOS) на Electron, которое берёт mp4-видеофайл, извлекает из него аудиодорожку и транскрибирует её в текст. Транскрипт сохраняется в `.md`-файл. После этого пользователь может отправить полученный текст в настраиваемую нейросетевую API (LLM) вместе с выбранным системным промптом, чтобы суммаризировать или проанализировать содержимое, посмотреть результат и сохранить его отдельным `.md`-файлом. Для себя и подобных пользователей, кому нужно быстро превращать видео в анализируемый текст.

## Core Value

Превратить mp4-видео в качественный текстовый транскрипт (`.md`) — это обязано работать, даже если всё остальное отвалится.

## Requirements

### Validated

<!-- Shipped and confirmed valuable. -->

(None yet — ship to validate)

### Active

<!-- Current scope. Building toward these. Все требования — гипотезы, пока не отгружены и не подтверждены. -->

- [ ] Пользователь может выбрать mp4-файл и приложение извлекает из него аудио
- [ ] Пользователь может транскрибировать аудио локально (Whisper на машине пользователя) — офлайн, бесплатно, приватно
- [ ] Пользователь может транскрибировать аудио через облачное API (например OpenAI Whisper) — на выбор в настройках
- [ ] Транскрипт сохраняется в `.md`-файл
- [ ] Пользователь может настроить подключение к нейро-API: готовые пресеты (OpenAI, Claude, Gemini) + кастомный OpenAI-совместимый endpoint (URL, ключ, модель)
- [ ] Пользователь может вести библиотеку именованных системных промптов и выбирать нужный перед отправкой
- [ ] Пользователь может отправить транскрипт + выбранный системный промпт в нейро-API и получить суммаризацию/анализ
- [ ] Результат анализа показывается в окне и сохраняется отдельным `.md`-файлом
- [ ] Приложение собирается и работает под Windows, Linux и macOS

### Out of Scope

<!-- Explicit boundaries. Includes reasoning to prevent re-adding. -->

- Пакетная обработка нескольких файлов — для v1 фокус на «один файл за раз», простота важнее
- Другие форматы видео/аудио кроме mp4 — сужаем v1, расширение возможно позже
- Редактор транскрипта с правкой текста — сначала генерация, правка не приоритет для v1
- Облачное хранилище / синхронизация — приложение локальное

## Context

- Аудио в обрабатываемых видео — преимущественно на русском языке; качество транскрипции на русском критично при выборе модели/настроек Whisper.
- Извлечение аудио из mp4 потребует ffmpeg (бандлинг или системная зависимость — решается на этапе ресёрча).
- Локальный Whisper требователен к ресурсам (CPU/GPU, размер модели); нужно дать пользователю выбор модели/качества.
- Поддержка кастомного OpenAI-совместимого endpoint позволяет подключать локальные LLM (Ollama, LM Studio) и сторонние провайдеры.
- Пользователь — технический, ведёт проекты по GSD-флоу (ветки feature/* от develop).

## Constraints

- **Tech stack**: Electron (кросс-платформенный десктоп) — выбор пользователя; JS/TS экосистема.
- **Compatibility**: должно собираться и работать на Windows, Linux и macOS.
- **Privacy**: локальный режим транскрипции должен работать полностью офлайн, без отправки данных вовне.
- **Dependencies**: ffmpeg для извлечения аудио; локальная Whisper-реализация (whisper.cpp / faster-whisper или аналог — определит ресёрч).

## Key Decisions

<!-- Decisions that constrain future work. Add throughout project lifecycle. -->

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Electron как фреймворк десктопа | Явный выбор пользователя; богатая экосистема, JS/TS везде | — Pending |
| Транскрипция на выбор: локальный Whisper ИЛИ облачное API | Баланс приватность/офлайн против скорости/простоты — решает пользователь | — Pending |
| Нейро-API: пресеты + кастомный endpoint | Гибкость: облачные провайдеры и локальные LLM через один интерфейс | — Pending |
| Системные промпты как библиотека именованных шаблонов | Переиспользование сценариев («Суммаризация», «Протокол встречи») | — Pending |
| v1 — один файл за раз | Простота фокуса, пакетная обработка отложена | — Pending |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd:complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-05-27 after initialization*
