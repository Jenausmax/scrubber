---
phase: 01-foundation-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - electron.vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - tsconfig.web.json
  - vitest.config.ts
  - tests/setup.ts
  - .gitignore
autonomous: true
requirements:
  - SHELL-01
user_setup: []

must_haves:
  truths:
    - "`npm run dev` запускает electron-vite + Electron из чистого клона репозитория"
    - "`npm run typecheck` зелёный"
    - "`npm run test:unit` исполняет vitest и проходит (даже если только smoke-тест)"
    - "Шаблон react-ts сгенерирован, версии стека приведены к CLAUDE.md"
  artifacts:
    - path: "package.json"
      provides: "ESM-проект, scripts dev/build/typecheck/test/test:unit, точные версии (electron@42.3.0, electron-vite@5.0.0, electron-builder@26.8.1, react@19, tailwindcss@4)"
      contains: "\"type\": \"module\""
    - path: "electron.vite.config.ts"
      provides: "main как ESM (format:'es'), Tailwind v4 через @tailwindcss/vite в renderer"
    - path: "vitest.config.ts"
      provides: "Vitest 2.x config, setupFiles: ['./tests/setup.ts'], environment node для main/preload"
    - path: "tests/setup.ts"
      provides: "vi.mock('electron') с фабрикой app/safeStorage/BrowserWindow/ipcMain/contextBridge/shell"
    - path: "tsconfig.web.json"
      contains: "../preload/index.d.ts"
  key_links:
    - from: "package.json"
      to: "electron-store@11"
      via: "ESM-only (\"type\": \"module\")"
      pattern: "\"type\": \"module\""
    - from: "tsconfig.web.json"
      to: "src/preload/index.d.ts"
      via: "include для типизации window.scrubber"
      pattern: "preload/index\\.d\\.ts"
---

<objective>
Поднять скелет проекта из официального шаблона `@quick-start/electron react-ts`, привести версии к CLAUDE.md, включить ESM, настроить Vitest 2.x с моком модуля `electron`, подготовить scripts. Этот план НЕ создаёт код приложения — только инфраструктуру, на которую опираются Wave 2/3/4.

Purpose: дать всем последующим планам зелёный typecheck/test/build конвейер и Wave 0 mock-инфраструктуру (см. 01-VALIDATION.md). Без этой основы юнит-тесты main-сервисов не запускаются вне Electron-рантайма.

Output: рабочий скелет + готовая тестовая инфраструктура. После плана `npm run dev` показывает дефолтное окно шаблона, `npm run typecheck` / `npm run test:unit` зелёные.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-app-shell/01-CONTEXT.md
@.planning/phases/01-foundation-app-shell/01-RESEARCH.md
@.planning/phases/01-foundation-app-shell/01-PATTERNS.md
@.planning/phases/01-foundation-app-shell/01-VALIDATION.md
@.planning/phases/01-foundation-app-shell/SKELETON.md
@CLAUDE.md
</context>

<phase_goal>
**As a** разработчик scrubber'а, **I want to** запустить `npm run dev` из свежего клона и увидеть Electron-окно с UI Settings, в котором сохранение API-ключа переживает рестарт, **so that** убеждён, что каркас (process boundaries + IPC + safeStorage) работает end-to-end на моей ОС до того, как поверх будут наслаиваться ffmpeg/whisper/LLM.
</phase_goal>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold react-ts + version pin + ESM</name>
  <read_first>
    - .planning/phases/01-foundation-app-shell/01-CONTEXT.md (D-01, D-02 — точные версии и шаблон)
    - .planning/phases/01-foundation-app-shell/01-PATTERNS.md §Config / build / tests
    - CLAUDE.md §Technology Stack (электорат версий)
  </read_first>
  <files>package.json, electron.vite.config.ts, tsconfig.json, tsconfig.node.json, tsconfig.web.json, .gitignore</files>
  <action>
    Сгенерировать скелет: `npm create @quick-start/electron@latest . -- --template react-ts --skip-install` (или интерактивно ответить scrubber/react-ts/no-eslint). После генерации:
    1. Привести версии в `package.json` к CLAUDE.md: `electron@42.3.0`, `electron-vite@5.0.0`, `electron-builder@26.8.1`, `react@^19`, `react-dom@^19`, `typescript@^5`, `tailwindcss@^4`, `@tailwindcss/vite@^4`. Удалить eslint/prettier если шаблон их подложил — в Phase 1 они не требуются.
    2. Добавить `"type": "module"` в `package.json` (Pitfall #3, D-13).
    3. Добавить scripts: `dev`, `build` (electron-vite build), `typecheck` (tsc --noEmit для node + web проекта), `test:unit` (vitest run --reporter=dot), `test` (typecheck && vitest run && electron-vite build).
    4. В `electron.vite.config.ts` убедиться, что main собирается как ESM (`build.rollupOptions.output.format = 'es'` или эквивалент конфигурации electron-vite 5). Подключить `@tailwindcss/vite()` в `renderer.plugins`.
    5. В `tsconfig.web.json` добавить в `include` пути `../preload/index.d.ts` и `../shared/**/*` (Pitfall #10). Если файлы ещё не существуют — оставить include, plan 02 их создаст.
    6. Запустить `npm install`.
    7. Запретить заглушку шаблона `src/` — оставить как есть; код будет заменён в Plan 02/04. Но создать пустой `src/shared/` и `src/main/services/`, `src/main/ipc/`, `src/renderer/src/routes/`, `src/renderer/src/components/` (чтобы tsconfig include не падал).
    8. `.gitignore`: убедиться, что покрыт `node_modules/`, `out/`, `dist/`, `*.bin`, `secrets.bin` (хотя `userData` снаружи репо, мера предосторожности).
    Запрещено: `keytar`, `@electron/remote`, `nodeIntegration`, encryption-опции electron-store (см. anti-pattern checklist в 01-PATTERNS.md).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: `package.json` содержит `"type": "module"` и точные версии из CLAUDE.md
    - source: `tsconfig.web.json` include содержит `../preload/index.d.ts` и `../shared/**/*`
    - behavior: `npm run typecheck` exit 0
    - cli: `node -e "const p=require('fs').readFileSync('package.json','utf8'); const j=JSON.parse(p); if(j.type!=='module') process.exit(1); if(j.dependencies?.keytar||j.devDependencies?.keytar) process.exit(2);"` exit 0
  </acceptance_criteria>
  <done>Скелет сгенерирован, версии приведены, ESM включён, scripts на месте, typecheck зелёный.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest 2.x + electron mock</name>
  <read_first>
    - .planning/phases/01-foundation-app-shell/01-VALIDATION.md (Wave 0 Requirements)
    - .planning/phases/01-foundation-app-shell/01-RESEARCH.md §Validation Architecture, §Wave 0 Gaps
    - .planning/phases/01-foundation-app-shell/01-PATTERNS.md §Config / build / tests
  </read_first>
  <files>vitest.config.ts, tests/setup.ts, package.json</files>
  <behavior>
    - Test 1: `import { app, safeStorage, BrowserWindow, ipcMain, contextBridge, shell } from 'electron'` в любом vitest-файле возвращает мок-объекты (а не выбрасывает «Cannot find module 'electron'»).
    - Test 2: `safeStorage.encryptString('x')` в моке возвращает `Buffer`-подобный объект, `safeStorage.decryptString(buf)` возвращает исходную строку.
    - Test 3: `safeStorage.getSelectedStorageBackend()` мокается per-test через `vi.mocked()` (по умолчанию возвращает `'gnome_libsecret'`).
    - Test 4: `app.getPath('userData')` возвращает временный путь, переопределяемый из теста.
  </behavior>
  <action>
    1. Создать `vitest.config.ts`: vitest 2.x, `test.environment = 'node'`, `test.setupFiles = ['./tests/setup.ts']`, `test.globals = false`, `test.alias = { '@shared': './src/shared', '@main': './src/main', '@renderer': './src/renderer/src' }` (или совместимый алиасинг electron-vite).
    2. Создать `tests/setup.ts`. Внутри — `vi.mock('electron', () => ({ ... }))` с фабрикой:
       - `app`: `getPath: vi.fn((name) => path.join(os.tmpdir(), 'scrubber-test', name))`, `whenReady: vi.fn().mockResolvedValue(undefined)`, `on: vi.fn()`, `quit: vi.fn()`.
       - `safeStorage`: `isEncryptionAvailable: vi.fn(() => true)`, `encryptString: vi.fn((s) => Buffer.from('enc:'+s)), `decryptString: vi.fn((b) => b.toString().replace(/^enc:/, ''))`, `getSelectedStorageBackend: vi.fn(() => 'gnome_libsecret')`.
       - `BrowserWindow`: класс с конструктором, фиксирующим переданные опции в `instance.__opts` (это потребуется в window.test.ts из Plan 02). Прототип: `loadURL`, `loadFile`, `webContents: { setWindowOpenHandler: vi.fn(), on: vi.fn() }`, `on: vi.fn()`.
       - `ipcMain`: `handle: vi.fn()`, `removeHandler: vi.fn()`.
       - `contextBridge`: `exposeInMainWorld: vi.fn()`.
       - `shell`: `openExternal: vi.fn().mockResolvedValue(undefined)`.
       - `utilityProcess`: пустая заглушка (понадобится в Phase 2).
    3. Экспортировать из `tests/setup.ts` хелпер `setBackend(backend: string)` который перепривязывает `getSelectedStorageBackend` (нужно для Plan 02 secure-backend.test).
    4. Установить dev-зависимости: `npm i -D vitest@^2 @vitest/coverage-v8` (если ещё нет в шаблоне).
    5. Создать `tests/smoke.test.ts` с одним проходящим тестом (`expect(1+1).toBe(2)`), чтобы `npm run test:unit` зелёный из коробки.
    Запрещено: watch-режим в скриптах (только `vitest run`), глобальные мутации process.platform без cleanup в afterEach.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - source: `vitest.config.ts` содержит `setupFiles: ['./tests/setup.ts']`
    - source: `tests/setup.ts` содержит `vi.mock('electron'` и экспорт `setBackend`
    - behavior: `npm run test:unit` exit 0, smoke-тест проходит
    - cli: `npx vitest run tests/smoke.test.ts --reporter=dot` exit 0
  </acceptance_criteria>
  <done>Vitest сконфигурирован, мок `electron` доступен глобально через setupFiles, smoke-тест зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a в Plan 01 | План создаёт только конфиги и тестовую инфраструктуру; runtime trust boundaries появляются с Plan 02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm install (`@quick-start/electron`, `electron`, `electron-vite`, `vitest`) | mitigate | Все пакеты — official-owned (npmjs.com publisher = `electron`, `alex8088`, `vitest`). Перед `npm install` исполнитель сверяется с CLAUDE.md §Technology Stack и анти-листом «What NOT to Use». `[ASSUMED]` пакетов нет — все легитимны. |
| T-01-CFG | Tampering | `package.json` сгенерированный шаблоном | mitigate | После `npm create` исполнитель ВРУЧНУЮ верифицирует отсутствие `keytar`, `@electron/remote`, `electron-store` с `encryptionKey`. Anti-pattern checklist из 01-PATTERNS.md обязателен. |
</threat_model>

<verification>
- `npm run typecheck` exit 0
- `npm run test:unit` exit 0
- `package.json` ESM + точные версии
- `tests/setup.ts` экспортирует мок `electron` со всеми нужными API
- Структура каталогов готова для Plan 02 (src/shared, src/main/services, src/main/ipc, src/renderer/src/routes, src/renderer/src/components)
</verification>

<success_criteria>
План завершён, когда: typecheck зелёный, test:unit зелёный, скелет соответствует D-01/D-02, ESM включён, Wave 0 тестовая инфраструктура готова к использованию в Plan 02–04.
</success_criteria>

<output>
Создать `.planning/phases/01-foundation-app-shell/01-01-SUMMARY.md` со списком установленных версий, расположением каталогов, и подтверждением Wave 0 mock-инфраструктуры.
</output>
