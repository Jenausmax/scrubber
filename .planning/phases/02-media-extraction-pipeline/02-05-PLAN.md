---
phase: 02-media-extraction-pipeline
plan: 05
type: execute
wave: 5
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - src/shared/ipc.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
  - src/renderer/src/components/DropZone.tsx
  - src/renderer/src/components/DropZone.test.tsx
  - src/main/services/ffmpeg-paths.ts
  - src/main/services/ffmpeg-paths.test.ts
  - src/main/services/media-extractor.ts
  - src/main/services/media-extractor.test.ts
  - src/renderer/src/components/InlineError.tsx
  - src/renderer/src/components/InlineError.test.tsx
  - tests/integration/extract-real.test.ts
  - scripts/smoke-packaged.mjs
  - package.json
  - .planning/phases/02-media-extraction-pipeline/02-05-SUMMARY.md
autonomous: false
requirements: [MEDIA-01, MEDIA-02, MEDIA-03]
gap_closure: true
closes_gaps:
  - gap_id: 1
    requirement: MEDIA-01
    file: 02-VERIFICATION.md
  - gap_id: 2
    requirement: MEDIA-02
    file: 02-VERIFICATION.md
  - gap_id: 3
    requirement: UX (через MEDIA-02 acceptance)
    file: 02-VERIFICATION.md

must_haves:
  truths:
    - "Drag-and-drop mp4-файла в DropZone приводит к вызову onPick(absPath) с непустым абсолютным путём — в packaged build на Windows (Electron 42, sandbox:true)."
    - "В packaged build (dist/win-unpacked/) ffmpeg- и ffprobe-бинарники физически существуют по путям, возвращаемым resolveFfmpeg()/resolveFfprobe(), и app.asar.unpacked присутствует в пути."
    - "При запуске media.extractAudio в packaged build на валидной mp4-фикстуре файл .wav создаётся в <userData>/extracted/<hash>.wav (Result.ok=true)."
    - "Каждый из 7 MediaReason-кодов (invalid_argument, not_mp4, file_not_found, ffmpeg_failed, cancelled, disk_full, internal) рендерится в уникальный русский текст в InlineError; никакие два reason'a не дают одну и ту же строку."
    - "Если ffmpeg-бинарник не существует/не запускается (spawn ENOENT), media-extractor возвращает reason='internal' с отдельным console.error-логом (не маскируется под 'ffmpeg_failed')."
    - "Standalone скрипт scripts/smoke-packaged.mjs (вызывается ОТДЕЛЬНО после build:unpack — host-OS-only) валит exit-code, если ffmpeg/ffprobe-бинарник физически отсутствует в app.asar.unpacked поддиректории."
    - "`ScrubberApi` в src/shared/ipc.ts расширен top-level полем `getPathForFile(file: File): string` — типобезопасный контракт preload↔renderer сохранён."
  artifacts:
    - path: "src/shared/ipc.ts"
      provides: "Расширенный тип ScrubberApi с top-level полем getPathForFile (контракт preload↔renderer)."
      contains: "getPathForFile"
    - path: "src/preload/index.ts"
      provides: "Экспонирование webUtils.getPathForFile через scrubber API (для Electron 32+ drag-drop)."
      contains: "getPathForFile"
    - path: "src/renderer/src/components/DropZone.tsx"
      provides: "handleDrop, который извлекает путь через window.scrubber.getPathForFile(file) вместо устаревшего file.path."
      contains: "getPathForFile"
    - path: "src/main/services/ffmpeg-paths.ts"
      provides: "resolveFfmpeg/resolveFfprobe + новая функция assertBinaryExists с fail-fast и явным console.error."
      contains: "assertBinaryExists"
    - path: "src/main/services/media-extractor.ts"
      provides: "spawn-error handler различает ENOENT (бинарник) → 'internal' от ffmpeg exit non-zero → 'ffmpeg_failed'."
      contains: "ENOENT"
    - path: "src/renderer/src/components/InlineError.tsx"
      provides: "REASON_COPY словарь покрывает все 7 кодов уникальными русскими строками (без коллизий)."
    - path: "tests/integration/extract-real.test.ts"
      provides: "Тест resolves-to-existing-file: existsSync(resolveFfmpeg()) && existsSync(resolveFfprobe()) в dev — guard на отсутствие бинарника в node_modules после install."
      contains: "existsSync"
    - path: "scripts/smoke-packaged.mjs"
      provides: "Standalone Node-скрипт; рекурсивно проверяет наличие ffmpeg/ffprobe бинарника per-OS в app.asar.unpacked после build:unpack. Вызывается отдельно (host-OS-only)."
      contains: "app.asar.unpacked"
  key_links:
    - from: "src/renderer/src/components/DropZone.tsx handleDrop"
      to: "src/preload/index.ts (window.scrubber.getPathForFile)"
      via: "Electron 32+ webUtils.getPathForFile(file)"
      pattern: "window\\.scrubber\\.getPathForFile"
    - from: "src/preload/index.ts contextBridge"
      to: "src/shared/ipc.ts ScrubberApi"
      via: "type-safe alignment ScrubberApi.getPathForFile"
      pattern: "ScrubberApi"
    - from: "src/main/services/media-extractor.ts utilityProcess.fork / spawn"
      to: "resolved ffmpeg/ffprobe binary on disk"
      via: "assertBinaryExists(resolveFfmpeg())"
      pattern: "assertBinaryExists"
    - from: "scripts/smoke-packaged.mjs"
      to: "dist/*-unpacked/resources/app.asar.unpacked/node_modules/{ffmpeg-static,@ffprobe-installer/*}/ffmpeg|ffprobe(.exe)"
      via: "fs.readdirSync recursive scan for binary file"
      pattern: "app\\.asar\\.unpacked"
---

<objective>
Закрыть 3 gap'a из 02-VERIFICATION.md, выявленные human smoke-тестом на Windows host:

- **Gap 1 (MEDIA-01):** drag-drop mp4-файла в DropZone не приводит к pick — причина: `File.path` удалён в Electron 32+ (мы на Electron 42, sandbox:true). Нужно перевести на `webUtils.getPathForFile(file)` через preload.
- **Gap 2 (MEDIA-02/03):** `extractAudio` падает в packaged build с «неподдерживаемый кодек» — нужно (a) добавить fail-fast `assertBinaryExists` на ffmpeg/ffprobe пути в `ffmpeg-paths.ts`; (b) добавить regression-guard в integration test (existsSync на resolved-путях); (c) создать `scripts/smoke-packaged.mjs` который ПОСЛЕ `build:unpack` (вызывается отдельно — host-OS-only) рекурсивно ищет бинарник `ffmpeg(.exe)` / `ffprobe(.exe)` в подпапках `app.asar.unpacked/node_modules/...` и валит exit-code если бинарник отсутствует.
- **Gap 3 (UX):** разделить spawn-ENOENT от ffmpeg-exit-non-zero в `media-extractor.ts` (первый → `internal`, второй → `ffmpeg_failed`); уточнить копии в `InlineError.tsx`, чтобы все 7 reason-кодов давали уникальные сообщения (проверить unit-тестом на отсутствие коллизий).

Purpose: после этого gap-closure повторный human smoke-test на Windows должен пройти полностью; regression-guard в integration + standalone smoke-скрипт ловят повтор Gap 2 без участия человека.

Output: 7 модифицированных source-файлов (включая `src/shared/ipc.ts` — расширение ScrubberApi), 3 модифицированных/новых test-файла, 1 новый smoke-скрипт, обновлённая документация smoke-инструкций (`dist/win-unpacked/`, не `out/win-unpacked/`).

Note про depends_on: формат `["02-01", "02-02", "02-03", "02-04"]` приведён к стандарту in-phase зависимостей фазы 02 (см. 02-02..02-04 — кавычки обязательны). План — gap-closure на УЖЕ merged 02-01..02-04; wave=5 ставится за wave=4 предыдущего плана.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-media-extraction-pipeline/02-VERIFICATION.md
@.planning/phases/02-media-extraction-pipeline/02-CONTEXT.md
@.planning/phases/02-media-extraction-pipeline/02-RESEARCH.md
@.planning/phases/02-media-extraction-pipeline/02-UI-SPEC.md
@.planning/phases/02-media-extraction-pipeline/02-04-SUMMARY.md
@src/shared/ipc.ts
@src/preload/index.ts
@src/renderer/src/components/DropZone.tsx
@src/renderer/src/components/InlineError.tsx
@src/main/services/ffmpeg-paths.ts
@src/main/services/media-extractor.ts
@tests/integration/extract-real.test.ts
@electron-builder.yml

<interfaces>
<!-- Ключевые контракты, которые executor должен сохранить — без обхода кодбазы. -->

MediaReason (src/shared/ipc.ts) — D-16:
type MediaReason =
  | 'invalid_argument'
  | 'not_mp4'
  | 'file_not_found'
  | 'ffmpeg_failed'
  | 'cancelled'
  | 'disk_full'
  | 'internal'

ScrubberApi (src/shared/ipc.ts) — текущий контракт ДО плана:
export interface ScrubberApi {
  settings: SettingsApi
  media: MediaApi
}
// → после Task 1 расширяется top-level полем:
//   getPathForFile: (file: File) => string

DropZone props (src/renderer/src/components/DropZone.tsx):
interface Props {
  onPick: (path: string) => void
  onError: (reason: MediaReason) => void
  onPickClick: () => void
  disabled?: boolean
  dragOver?: boolean
  onDragOver?: (e: DragEvent<HTMLDivElement>) => void
  onDragLeave?: (e: DragEvent<HTMLDivElement>) => void
}

Electron 32+ drag-drop API (root cause Gap 1):
// preload (CommonJS bundle, contextBridge):
import { webUtils } from 'electron'
contextBridge.exposeInMainWorld('scrubber', {
  ...,
  getPathForFile: (file: File) => webUtils.getPathForFile(file),
  ...
})
// renderer:
const filePath = window.scrubber.getPathForFile(file)  // вместо (f as any).path

ffmpeg-paths.ts — текущий контракт (сохранить + расширить):
export function resolveFfmpeg(): string  // .replace('app.asar','app.asar.unpacked')
export function resolveFfprobe(): string
export async function ensureExecutable(p: string): Promise<void>
// NEW:
export function assertBinaryExists(p: string, name: string): void
  // synchronous existsSync; if false → throw Error с понятным сообщением
  // (включая sanitized path) + console.error префиксом [services/ffmpeg-paths].

InlineError REASON_COPY — текущий (сохранить структуру, уточнить тексты):
const REASON_COPY: Record<MediaReason, string> = { ... 7 ключей ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gap 1 fix — webUtils.getPathForFile в preload + DropZone + расширение ScrubberApi</name>
  <files>
    src/shared/ipc.ts,
    src/preload/index.ts,
    src/preload/index.d.ts,
    src/renderer/src/components/DropZone.tsx,
    src/renderer/src/components/DropZone.test.tsx
  </files>
  <read_first>
    - src/shared/ipc.ts (текущий тип ScrubberApi — namespaces settings + media)
    - src/preload/index.ts (текущий API window.scrubber; `const scrubber: ScrubberApi = {...}` — typecheck должен пройти)
    - src/preload/index.d.ts (типизация renderer-side)
    - src/renderer/src/components/DropZone.tsx (текущая логика handleDrop с (f as any).path)
    - .planning/phases/02-media-extraction-pipeline/02-CONTEXT.md (D-14 sandbox, D-06/07 валидация)
    - .planning/phases/02-media-extraction-pipeline/02-RESEARCH.md §Pattern 2 (drag&drop)
    - .planning/phases/02-media-extraction-pipeline/02-VERIFICATION.md Gap 1
  </read_first>
  <action>
    Root cause Gap 1: `File.path` удалён в Electron 32+ (мы на electron@42.3.0 + sandbox:true). Нужно перейти на `webUtils.getPathForFile(file)`. Так как preload типизируется через `const scrubber: ScrubberApi = {...}`, контракт `ScrubberApi` в `src/shared/ipc.ts` нужно расширить ПЕРВЫМ — иначе typecheck свалится.

    1. **src/shared/ipc.ts — расширить ScrubberApi (выполнить ПЕРВЫМ шагом):**
       В существующий `export interface ScrubberApi` добавить top-level поле:
       ```
       /**
        * Electron 32+ drag-drop fix (02-05 Gap 1).
        * `File.path` удалён в Electron ≥32; renderer получает абсолютный путь
        * только через `webUtils.getPathForFile(file)` в preload.
        * Это renderer-side утилита, НЕ IPC — без Result-обёртки, синхронная.
        */
       getPathForFile: (file: File) => string
       ```
       Поместить как top-level рядом с `settings` и `media` (не внутрь `media`). Сохранить порядок и комментарии остальных полей.

    2. **src/preload/index.ts:** добавить импорт `import { webUtils } from 'electron'` (CJS — sandboxed preload). В `contextBridge.exposeInMainWorld('scrubber', {...})` (или в `const scrubber: ScrubberApi = {...}`) добавить top-level поле `getPathForFile: (file: File): string => webUtils.getPathForFile(file)`. Поместить рядом с `settings` и `media`, не внутрь `media.*`. Сохранить существующий список полей, ничего не удалять. После этого `const scrubber: ScrubberApi = {...}` typecheck должен совпасть с обновлённым типом из шага 1.

    3. **src/preload/index.d.ts:** расширить тип global `Window['scrubber']` полем `getPathForFile(file: File): string` (для типизации в renderer без импорта из shared).

    4. **src/renderer/src/components/DropZone.tsx**, функция `handleDrop`: заменить
       `const filePath = (f as File & { path?: string }).path`
       на
       `const filePath = window.scrubber.getPathForFile(f)`.
       Оставить логику валидации (length === 0 → onError('internal')) без изменений. Сохранить расширение-чек `.mp4` и multi-drop reject. Удалить TS-каст `as File & { path?: string }`.

    5. **src/renderer/src/components/DropZone.test.tsx** (если файла нет — создать; jsdom + @testing-library/react из package.json):
       - Замокать `window.scrubber.getPathForFile = vi.fn().mockReturnValue('C:\\Users\\test\\video.mp4')` в `beforeEach`.
       - Тест 1: при drop одного `.mp4`-файла handler `onPick` вызывается с этим путём.
       - Тест 2: при drop НЕ-`.mp4` — `onError('not_mp4')`, `onPick` НЕ вызывается.
       - Тест 3: при drop >1 файлов — `onError('invalid_argument')`.
       - Тест 4: при drop, но `getPathForFile` вернул '' — `onError('internal')`.
       - Использовать `fireEvent.drop(zone, { dataTransfer: { files: [file] } })` (или `dataTransfer` mock через `Object.defineProperty` если jsdom не поддерживает напрямую). Для нескольких файлов передать массив.
       Использовать существующие паттерны из `src/renderer/src/routes/Transcribe.test.tsx` (там есть пример stub'а на `window.scrubber`).

    NB: не трогать рендер/ARIA/Tailwind-классы. Не добавлять новые reason-коды. Не трогать `media`/`settings` namespaces в `ScrubberApi`.
  </action>
  <verify>
    <automated>npx vitest run src/renderer/src/components/DropZone.test.tsx src/renderer/src/routes/Transcribe.test.tsx --reporter=dot</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getPathForFile" src/shared/ipc.ts` возвращает минимум 1 строку (top-level в ScrubberApi).
    - `grep -n "getPathForFile" src/preload/index.ts` возвращает минимум 2 строки (import + expose).
    - `grep -n "getPathForFile" src/renderer/src/components/DropZone.tsx` возвращает минимум 1 строку.
    - `grep -nE "\\(f as File & \\{ path\\?: string \\}\\)" src/renderer/src/components/DropZone.tsx` возвращает 0 строк (старый каст удалён).
    - `grep -n "getPathForFile" src/preload/index.d.ts` возвращает минимум 1 строку.
    - DropZone.test.tsx содержит минимум 4 теста (Drop happy, Drop non-mp4, Drop multi, Drop empty-path).
    - `npx vitest run src/renderer/src/components/DropZone.test.tsx --reporter=dot` — все тесты зелёные.
    - `npm run typecheck` зелёный (включая `const scrubber: ScrubberApi = {...}` в preload).
  </acceptance_criteria>
  <done>
    `ScrubberApi` расширен top-level полем `getPathForFile`; preload экспонирует `webUtils.getPathForFile`; drag-drop mp4-файла в DropZone (Windows packaged build, Electron 42) приводит к вызову onPick с абсолютным путём; unit-тесты на DropZone покрывают 4 поведенческих случая; typecheck зелёный.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gap 2 fix — assertBinaryExists + spawn-ENOENT mapping + regression-guard в integration</name>
  <files>
    src/main/services/ffmpeg-paths.ts,
    src/main/services/ffmpeg-paths.test.ts,
    src/main/services/media-extractor.ts,
    src/main/services/media-extractor.test.ts,
    tests/integration/extract-real.test.ts
  </files>
  <read_first>
    - src/main/services/ffmpeg-paths.ts (resolveFfmpeg/resolveFfprobe/ensureExecutable)
    - src/main/services/ffmpeg-paths.test.ts (паттерны vi.doMock на ffmpeg-static)
    - src/main/services/media-extractor.ts (init, probe, startExtract — где spawn падает)
    - tests/integration/extract-real.test.ts (текущая структура интеграционного теста)
    - electron-builder.yml (asarUnpack уже корректный — guard ловит регрессию)
    - .planning/phases/02-media-extraction-pipeline/02-VERIFICATION.md Gap 2 (hypothesis 1)
    - .planning/phases/02-media-extraction-pipeline/02-RESEARCH.md §Pitfall #1
  </read_first>
  <action>
    Root cause Gap 2 (приоритет): на packaged build `resolveFfmpeg().replace('app.asar','app.asar.unpacked')` возвращает путь, но мы не проверяем, что файл по этому пути ФИЗИЧЕСКИ существует — если `asarUnpack` сломан/электрон-билдер изменил layout/install-app-deps не отработал, мы получаем тихое падение spawn с misleading reason 'ffmpeg_failed'. Решение — fail-fast с явным сообщением + regression-тест.

    1. **src/main/services/ffmpeg-paths.ts** — добавить:
       ```
       import { existsSync } from 'node:fs'

       export function assertBinaryExists(p: string, name: string): void {
         if (!existsSync(p)) {
           const msg = `[services/ffmpeg-paths] ${name} binary not found at resolved path: ${p}. ` +
             `Check electron-builder asarUnpack config and ensure 'npm run postinstall' ` +
             `(electron-builder install-app-deps) completed successfully.`
           console.error(msg)
           throw new Error(msg)
         }
       }
       ```
       Экспорт добавить рядом с существующими. Не трогать resolveFfmpeg/resolveFfprobe/ensureExecutable.

    2. **src/main/services/ffmpeg-paths.test.ts** — добавить describe-блок `assertBinaryExists`:
       - Тест A: `existsSync` возвращает true → НЕ throw. Замокать `node:fs` так же, как в существующем `ensureExecutable`-блоке (через `vi.doMock('node:fs', () => ({ existsSync: vi.fn().mockReturnValue(true), promises: {...}, constants: {...} }))`). Импортировать `assertBinaryExists` через `await import('./ffmpeg-paths')`.
       - Тест B: `existsSync` возвращает false → throw `Error` с substring `ffmpeg binary not found` и substring переданного пути.
       - Тест C: throw содержит подсказку про `asarUnpack` (substring `asarUnpack`).
       Использовать существующие vi.doMock паттерны из файла (resetModules между тестами).

    3. **src/main/services/media-extractor.ts** — править `init()`:
       ```
       async init(): Promise<void> {
         if (this.initialised) return
         this.cacheDir = join(app.getPath('userData'), 'extracted')
         await fs.mkdir(this.cacheDir, { recursive: true })
         const ffPath = resolveFfmpeg()
         const fpPath = resolveFfprobe()
         assertBinaryExists(ffPath, 'ffmpeg')         // <-- NEW
         assertBinaryExists(fpPath, 'ffprobe')        // <-- NEW
         await ensureExecutable(ffPath)
         await ensureExecutable(fpPath)
         this.initialised = true
       }
       ```
       (Импорт `assertBinaryExists` из `./ffmpeg-paths`.)

       Дополнительно в `startExtract`: обернуть `utilityProcess.fork(scriptPath, ...)` так, чтобы если `proc.on('error', err => ...)` поймал `ENOENT` от spawn — Promise резолвится с `{ ok: false, reason: 'internal' }` (НЕ `ffmpeg_failed`). Сейчас exit-code != 0 даёт `ffmpeg_failed` — оставить как есть, но spawn-error (когда сам бинарник/cjs-скрипт не запустился) считать `internal`. Логировать `console.error(LOG_PREFIX, 'spawn error:', err)`. Если в коде уже есть `proc.on('error', ...)` — обновить ветку с `code === 'ENOENT'`.

    4. **src/main/services/media-extractor.test.ts** — добавить тест: `init() throws when assertBinaryExists fails`. Замокать `./ffmpeg-paths` через `vi.doMock`, заставить `assertBinaryExists` бросать Error, импортировать MediaExtractor, ожидать `await expect(extractor.init()).rejects.toThrow(/binary not found/)`. Использовать существующие моки `app.getPath` из файла (mock electron).

    5. **tests/integration/extract-real.test.ts** — добавить регрессионный тест в существующий describe:
       ```
       it('regression Gap-2: resolved ffmpeg/ffprobe binaries physically exist (dev path)', async () => {
         const { resolveFfmpeg, resolveFfprobe } = await import('../../src/main/services/ffmpeg-paths')
         const { existsSync } = await import('node:fs')
         expect(existsSync(resolveFfmpeg())).toBe(true)
         expect(existsSync(resolveFfprobe())).toBe(true)
       })
       ```
       Этот тест работает в dev (бинарники лежат в `node_modules/ffmpeg-static/`, replace на `.unpacked` — no-op для dev-путей, потому что `app.asar` подстроки нет). В packaged build тест НЕ запускается (vitest гоняется по dev-tree); regression для packaged build ловится `scripts/smoke-packaged.mjs` из Task 4.

    NB: НЕ менять JSON-структуру MediaProbeResult/MediaExtractResult. НЕ добавлять новые reason-коды (Gap 3 решается уточнением копий, не расширением enum). НЕ трогать `electron-builder.yml`.
  </action>
  <verify>
    <automated>npx vitest run src/main/services/ffmpeg-paths.test.ts src/main/services/media-extractor.test.ts --reporter=dot</automated>
    <automated>npx vitest run tests/integration/extract-real.test.ts --reporter=dot --no-file-parallelism</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "assertBinaryExists" src/main/services/ffmpeg-paths.ts` >= 2 строки (export + impl).
    - `grep -n "assertBinaryExists" src/main/services/media-extractor.ts` >= 2 строки (import + 2 вызова в init).
    - `grep -n "existsSync" src/main/services/ffmpeg-paths.ts` >= 1.
    - `grep -nE "describe\\(['\"]assertBinaryExists" src/main/services/ffmpeg-paths.test.ts` возвращает 1 совпадение.
    - В `tests/integration/extract-real.test.ts` присутствует тест с substring `'regression Gap-2'` и вызовом `existsSync(resolveFfmpeg())`.
    - Все vitest-команды в `<verify>` зелёные.
    - `npm run typecheck` зелёный.
    - Регрессия imitate: если временно сломать `assertBinaryExists` на `throw new Error('forced')` в test-моке — `init` падает с message содержащим `binary not found` (тест на это есть).
  </acceptance_criteria>
  <done>
    `MediaExtractor.init()` fail-fast'ит с понятным сообщением, если ffmpeg/ffprobe бинарники не существуют по resolved-путям; spawn-ENOENT в startExtract мапится на `internal`, а не `ffmpeg_failed`; integration-тест guard'ит существование бинарников в dev-build.
  </done>
</task>

<task type="auto">
  <name>Task 3: Gap 3 fix — уточнить REASON_COPY и unit-тест на уникальность всех 7 строк</name>
  <files>
    src/renderer/src/components/InlineError.tsx,
    src/renderer/src/components/InlineError.test.tsx
  </files>
  <read_first>
    - src/renderer/src/components/InlineError.tsx (текущий REASON_COPY)
    - src/shared/ipc.ts (полный список MediaReason)
    - .planning/phases/02-media-extraction-pipeline/02-UI-SPEC.md §Copywriting Contract
    - .planning/phases/02-media-extraction-pipeline/02-CONTEXT.md D-16
    - .planning/phases/02-media-extraction-pipeline/02-VERIFICATION.md Gap 3
  </read_first>
  <action>
    Сейчас `InlineError.tsx` уже определяет REASON_COPY на все 7 кодов, но human smoke-test показал «неподдерживаемый кодек» — это копия `ffmpeg_failed`. После Task 2 spawn-ENOENT уже не маскируется под ffmpeg_failed, но текст для `internal` слишком общий («Внутренняя ошибка. Попробуйте перезапустить»). Нужно:

    1. **src/renderer/src/components/InlineError.tsx** — заменить REASON_COPY на финальные русские строки (взять из UI-SPEC §Copywriting Contract; уточнить там, где маппинг был слишком общий):
       ```
       const REASON_COPY: Record<MediaReason, string> = {
         invalid_argument: 'Можно перетащить только один mp4-файл за раз.',
         not_mp4: 'Поддерживается только формат .mp4. Выберите другой файл.',
         file_not_found: 'Файл не найден. Возможно, он был перемещён или удалён.',
         ffmpeg_failed: 'Не удалось извлечь аудио: файл повреждён или содержит неподдерживаемый кодек.',
         cancelled: 'Извлечение отменено пользователем.',
         disk_full: 'Недостаточно места на диске для сохранения аудио. Освободите место и попробуйте снова.',
         internal: 'Внутренняя ошибка приложения. Проверьте установку: бинарник ffmpeg может быть не распакован. Перезапустите приложение.'
       }
       ```
       Каждая строка обязана отличаться от остальных шести (включая первые 10 символов — для grep-проверки).

    2. **src/renderer/src/components/InlineError.test.tsx** — создать (если нет) с тестами:
       - Тест 1: для каждого из 7 MediaReason-кодов render `<InlineError reason={code} onRetry={vi.fn()}>` — `screen.getByRole('alert')` содержит непустой текст.
       - Тест 2 (uniqueness): собрать массив текстов для всех 7 кодов, проверить `new Set(texts).size === 7` — нет коллизий.
       - Тест 3: клик по кнопке «Попробовать снова» вызывает `onRetry`.
       Использовать `@testing-library/react` (уже в devDeps), pattern из существующих тестов в `src/renderer/src/`.

    3. **Документация**: добавить в начало `InlineError.tsx` комментарий-source: `// 02-05-PLAN Task 3 — точные копи уточнены, uniqueness покрыта unit-тестом.` Не удалять существующий комментарий-источник.

    NB: НЕ менять `MediaReason` type (Task 2 уже решил разделение spawn vs exit через mapping в main-process). НЕ добавлять подкоды.
  </action>
  <verify>
    <automated>npx vitest run src/renderer/src/components/InlineError.test.tsx --reporter=dot</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^[[:space:]]+(invalid_argument|not_mp4|file_not_found|ffmpeg_failed|cancelled|disk_full|internal):" src/renderer/src/components/InlineError.tsx` возвращает ровно 7.
    - Unit-тест uniqueness (`new Set(texts).size === 7`) зелёный.
    - Каждый из 7 codes в тесте 1 рендерится с непустым innerText (>= 10 символов).
    - Текст для `internal` отличается от текста для `ffmpeg_failed` минимум на 20 символов substring-diff.
    - Текст для `cancelled` отличается от любых других (Set-uniqueness).
    - `npm run typecheck` зелёный.
  </acceptance_criteria>
  <done>
    Все 7 MediaReason-кодов имеют уникальные осмысленные русские сообщения; unit-тест гарантирует отсутствие коллизий; копия для `internal` явно намекает на ffmpeg-binary install issue (помощь юзеру при повторе Gap 2).
  </done>
</task>

<task type="auto">
  <name>Task 4: Smoke-скрипт packaged build (standalone, host-OS-only) + правка документации</name>
  <files>
    scripts/smoke-packaged.mjs,
    package.json
  </files>
  <read_first>
    - electron-builder.yml (asarUnpack секция — какие пути проверять)
    - package.json (scripts.build:unpack, scripts.build:win — НЕ менять chain, только добавить standalone скрипт)
    - .planning/phases/02-media-extraction-pipeline/02-VERIFICATION.md Notes (path discrepancy dist/ vs out/)
  </read_first>
  <action>
    Создать standalone Node-скрипт, который запускается ВРУЧНУЮ (или в host-OS CI job) ПОСЛЕ `npm run build:unpack` и валит exit-code, если в packaged build отсутствуют распакованные ffmpeg/ffprobe бинарники. Это автоматизирует regression-guard для Gap 2.

    **Решение по W-4 (CI fragility):** оставить `smoke:packaged` ОТДЕЛЬНОЙ npm-командой. `build:unpack` НЕ цепляется через `&&` — `electron-builder --dir` сам по себе host-OS-only (на macOS не соберёт win-target без cross-compile setup), и зашивать smoke в chain делает любую попытку запустить `build:unpack` на «чужой» OS красной. Smoke вызывается отдельно после успешного `build:unpack` на host-OS.

    1. **scripts/smoke-packaged.mjs** — создать новый файл:
       ```
       #!/usr/bin/env node
       // Standalone smoke-check для packaged build. Запускать ВРУЧНУЮ после `npm run build:unpack`
       // на host-OS (Win/Linux/macOS — соответствующая target). НЕ цепляется в build:unpack chain
       // (host-OS-only сценарий, см. 02-05-PLAN W-4).
       //
       // Проверяет, что electron-builder корректно распаковал ffmpeg/ffprobe бинарники
       // в app.asar.unpacked (рекурсивный поиск файла, а не только директории).
       // Закрывает regression-guard для Gap 2 из 02-VERIFICATION.md.
       import { existsSync, readdirSync, statSync } from 'node:fs'
       import { join } from 'node:path'
       import { platform } from 'node:os'

       const ROOT = process.cwd()
       const distDir = join(ROOT, 'dist')
       if (!existsSync(distDir)) {
         console.error('[smoke] dist/ does not exist — run `npm run build:unpack` first')
         process.exit(2)
       }

       // Найти первую подпапку *-unpacked в dist/
       const entries = readdirSync(distDir, { withFileTypes: true })
       const unpacked = entries.find(e => e.isDirectory() && /-unpacked$/.test(e.name))
       if (!unpacked) {
         console.error('[smoke] no *-unpacked directory in dist/ — build:unpack failed?')
         process.exit(3)
       }

       const unpackedRoot = join(distDir, unpacked.name)
       const asarUnpackedRoot = join(unpackedRoot, 'resources', 'app.asar.unpacked')
       const ffmpegStaticDir = join(asarUnpackedRoot, 'node_modules', 'ffmpeg-static')
       const ffprobeBaseDir = join(asarUnpackedRoot, 'node_modules', '@ffprobe-installer')

       const isWin = platform() === 'win32'
       const ffmpegBinName = isWin ? 'ffmpeg.exe' : 'ffmpeg'
       const ffprobeBinName = isWin ? 'ffprobe.exe' : 'ffprobe'

       /**
        * Рекурсивно ищет файл с именем `name` в дереве, начиная с `root`.
        * Возвращает абсолютный путь к первому найденному или null.
        * Глубина ограничена 4 уровнями (защита от циклов/глубоких node_modules).
        */
       function findBinary(root, name, depth = 0) {
         if (!existsSync(root) || depth > 4) return null
         let entries
         try { entries = readdirSync(root, { withFileTypes: true }) } catch { return null }
         for (const e of entries) {
           const full = join(root, e.name)
           if (e.isFile() && e.name === name) return full
           if (e.isDirectory()) {
             const found = findBinary(full, name, depth + 1)
             if (found) return found
           }
         }
         return null
       }

       const failures = []
       if (!existsSync(asarUnpackedRoot)) {
         failures.push(`app.asar.unpacked NOT FOUND at ${asarUnpackedRoot}`)
       }

       // ffmpeg: бинарник лежит в ffmpeg-static/ напрямую
       if (existsSync(ffmpegStaticDir)) {
         const ffmpegPath = join(ffmpegStaticDir, ffmpegBinName)
         if (!existsSync(ffmpegPath)) {
           failures.push(`ffmpeg binary missing at ${ffmpegPath}`)
         }
       } else {
         failures.push(`ffmpeg-static dir NOT unpacked at ${ffmpegStaticDir}`)
       }

       // ffprobe: бинарник лежит в @ffprobe-installer/<platform>-<arch>/ — рекурсивный поиск
       if (existsSync(ffprobeBaseDir)) {
         const ffprobePath = findBinary(ffprobeBaseDir, ffprobeBinName)
         if (!ffprobePath) {
           failures.push(
             `${ffprobeBinName} binary not found anywhere under ${ffprobeBaseDir} ` +
             `(expected in per-OS subpackage e.g. @ffprobe-installer/win32-x64/)`
           )
         } else {
           // Доп. проверка: бинарник не пустой
           try {
             if (statSync(ffprobePath).size < 1024) {
               failures.push(`${ffprobeBinName} binary suspiciously small (<1KB) at ${ffprobePath}`)
             }
           } catch { /* ignore stat errors */ }
         }
       } else {
         failures.push(`@ffprobe-installer dir NOT unpacked at ${ffprobeBaseDir}`)
       }

       if (failures.length > 0) {
         console.error('[smoke] PACKAGED BUILD CHECK FAILED:')
         for (const f of failures) console.error('  - ' + f)
         console.error('\nFix: проверь electron-builder.yml asarUnpack секцию + перезапусти `npm run build:unpack`.')
         process.exit(1)
       }
       console.log(`[smoke] OK: ffmpeg + ffprobe бинарники найдены в ${asarUnpackedRoot}`)
       process.exit(0)
       ```

    2. **package.json** — добавить новый npm-скрипт `smoke:packaged` рядом с `build:unpack`:
       ```
       "smoke:packaged": "node scripts/smoke-packaged.mjs"
       ```
       **НЕ менять `build:unpack`** — оставить как есть (`"build && electron-builder --dir"` или текущая форма). `smoke:packaged` вызывается отдельно: `npm run build:unpack && npm run smoke:packaged` (на host-OS). Это решение фиксирует W-4 (вариант a): чище, без CI cross-OS-сюрпризов.

    NB: скрипт ESM (`.mjs`) — Node 20+ из tsconfig.node.json. НЕ зависит от electron. НЕ требует typecheck (vanilla JS). НЕ удалять/перестраивать другие npm-скрипты.
  </action>
  <verify>
    <automated>node -c scripts/smoke-packaged.mjs</automated>
    <automated>node scripts/smoke-packaged.mjs ; echo "exit=$?"</automated>
    <automated>grep -E '"smoke:packaged":\s*"node scripts/smoke-packaged\.mjs"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - Файл `scripts/smoke-packaged.mjs` существует, начинается с shebang `#!/usr/bin/env node` и `import { existsSync` (валидный ESM).
    - `node -c scripts/smoke-packaged.mjs` (syntax-check) проходит без ошибок (exit 0).
    - При запуске БЕЗ выполненного build:unpack (dist/ нет ИЛИ нет *-unpacked) скрипт exit-code != 0 и пишет `[smoke]` в stderr.
    - `grep -E '"smoke:packaged":\s*"node scripts/smoke-packaged\.mjs"' package.json` → exit 0 (одна строка matches).
    - `grep -nE "findBinary|readdirSync" scripts/smoke-packaged.mjs` >= 2 (рекурсивный поиск присутствует — закрытие W-2).
    - `grep -nE "ffprobe(\.exe)?" scripts/smoke-packaged.mjs` >= 2 (проверка именно бинарника, не только директории).
    - `grep -n "dist/\\*-unpacked\\|app\\.asar\\.unpacked" scripts/smoke-packaged.mjs` >= 1 (документирует ожидаемый layout).
    - `grep -n '"build:unpack":.*smoke:packaged' package.json` возвращает 0 строк (chain НЕ применён — W-4 вариант a).
  </acceptance_criteria>
  <done>
    `npm run smoke:packaged` (отдельная команда, host-OS-only) валит exit-code, если ffmpeg/ffprobe-бинарник не распакован в `dist/*-unpacked/resources/app.asar.unpacked/node_modules/...` (рекурсивный поиск). Regression-guard для Gap 2 покрыт без участия человека и без CI cross-OS-фрагильности.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human smoke-test packaged build на Windows host (повтор после fix'ов)</name>
  <what-built>
    Three gaps closed:
    - Gap 1: drag-drop через `webUtils.getPathForFile` (Electron 32+ API) + расширение ScrubberApi
    - Gap 2: `assertBinaryExists` + integration-regression-тест + `scripts/smoke-packaged.mjs` (standalone)
    - Gap 3: уточнённые русские копии для всех 7 reason-кодов
  </what-built>
  <how-to-verify>
    На Windows host:

    1. **Clean build:**
       ```
       npm run build:unpack
       ```
       Ожидание: команда завершается успешно, создаётся `dist/win-unpacked/`.

    2. **Smoke-check (отдельная команда):**
       ```
       npm run smoke:packaged
       ```
       Ожидание: `[smoke] OK: ffmpeg + ffprobe бинарники найдены в .../dist/win-unpacked/resources/app.asar.unpacked`.
       Если падает с exit != 0 — Gap 2 не закрыт, описать stderr в resume-signal.

    3. **Запуск packaged app:**
       Перейти в `dist/win-unpacked/` и запустить `.exe` (имя из `electron-builder.yml win.executableName: react-ts` → `react-ts.exe`).

    4. **Gap 1 verification — drag-drop:**
       - Перетащить любой mp4-файл (например тот, что использовался в smoke ранее) в drop-zone на вкладке «Транскрипция».
       - Ожидание: появляется FileMetaCard с meta (duration, size, name), CTA «Извлечь аудио».
       - Если drag-drop НЕ работает — сообщить «Gap 1 не закрыт» в resume-signal.

    5. **Gap 2 verification — extract:**
       - Нажать «Извлечь аудио».
       - Ожидание: progress-bar идёт от 0 до 100%, по окончании появляется ExtractDone с путём `<userData>/extracted/<hash>.wav`.
       - Если получаем error-карточку «не удалось извлечь» — сообщить «Gap 2 не закрыт» + текст ошибки.

    6. **Gap 3 verification — error copies:**
       - Spawn-error: временно переименовать `dist/win-unpacked/resources/app.asar.unpacked/node_modules/ffmpeg-static/ffmpeg.exe` → `ffmpeg.exe.bak`, перезапустить app, попытаться extract.
       - Ожидание: при init() app падает с понятным console-сообщением (см. DevTools / лог); ИЛИ UI показывает текст для `internal` с упоминанием «бинарник ffmpeg может быть не распакован». НЕ должен показываться текст «неподдерживаемый кодек».
       - Откатить переименование: `ffmpeg.exe.bak` → `ffmpeg.exe`.

    7. **Скопировать путь:** на done-state нажать «Скопировать путь» — clipboard содержит `<userData>/extracted/<hash>.wav`.

    8. **Resume-signal:** `approved` если все 3 gap'a закрыты; иначе подробное описание (какой gap, какое наблюдаемое поведение).
  </how-to-verify>
  <resume-signal>
    Type `approved` if all 3 gaps closed end-to-end on Windows packaged build, otherwise describe which gap still fails and the observed behavior.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → preload | Drag-drop файлов; renderer не имеет прямого `fs`/`node:*`. |
| main → child_process (ffmpeg/ffprobe) | Spawn нативных бинарников с user-controlled путём как аргументом. |
| packaging → installation | electron-builder asarUnpack резолвится корректно на target OS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information disclosure | `webUtils.getPathForFile` в preload | accept | API возвращает только path string — содержимое файла не читается через этот канал; путь и так нужен main-процессу для spawn. Sandboxed preload, ContextBridge. |
| T-02-05-02 | Tampering | `assertBinaryExists` стек-трейс с full path в console | mitigate | Error message содержит resolved path — это локальный лог, не отправляется наружу. Логировать через `console.error` (main-process, dev/prod), не в renderer. |
| T-02-05-03 | Denial of service | spawn ENOENT при missing ffmpeg бинарнике | mitigate | `init()` fail-fast'ит при старте app, `startExtract` returns Result.ok=false reason='internal' — UI показывает retry-кнопку, не зависает. |
| T-02-05-04 | Spoofing | `webUtils.getPathForFile(file)` — путь может быть подменён? | accept | Electron core API; user сам кладёт mp4 на drop-zone; main-process делает повторную валидацию (isAbsolute + fs.stat) перед spawn (defence-in-depth, D-06). |
| T-02-05-SC | Tampering | npm installs | n/a | Никаких новых npm-зависимостей. Все правки — в существующем коде. `webUtils` экспортирован из встроенного модуля `electron` (electron@42.3.0, проверено в release notes). Slopcheck не требуется. |
</threat_model>

<verification>
- Все vitest-команды из `<verify>` каждой задачи зелёные.
- `npm run typecheck` зелёный.
- `node -c scripts/smoke-packaged.mjs` без syntax-error.
- `npm run build:unpack` на Windows host завершается успешно; `npm run smoke:packaged` отдельно завершается с `[smoke] OK:`.
- Human smoke-test (Task 5) подтверждает all 3 gaps closed.
- Integration `tests/integration/extract-real.test.ts` содержит regression-тест Gap-2 и зелёный.
- Сохраняются все 112 ранее зелёных тестов: `npx vitest run --no-file-parallelism --reporter=dot` >= 112+ passed.
</verification>

<success_criteria>
- [ ] `ScrubberApi` в `src/shared/ipc.ts` расширен top-level полем `getPathForFile(file: File): string` (typecheck preload зелёный).
- [ ] `window.scrubber.getPathForFile` экспонирован через preload и используется в DropZone (Gap 1).
- [ ] `assertBinaryExists` вызывается в `MediaExtractor.init()` для ffmpeg и ffprobe (Gap 2).
- [ ] `tests/integration/extract-real.test.ts` содержит regression-тест `existsSync(resolveFfmpeg())` (Gap 2).
- [ ] `scripts/smoke-packaged.mjs` рекурсивно ищет именно бинарник `ffmpeg(.exe)` / `ffprobe(.exe)` в подпапках `app.asar.unpacked/node_modules/...` и валит exit-code при отсутствии (Gap 2, W-2 закрыт).
- [ ] `smoke:packaged` существует как отдельный npm-script; `build:unpack` НЕ цепляет `smoke:packaged` через `&&` (W-4 вариант a — host-OS-only standalone).
- [ ] `REASON_COPY` в `InlineError.tsx` — 7 уникальных русских строк, покрыты unit-тестом uniqueness (Gap 3).
- [ ] Spawn ENOENT в `media-extractor.ts` маппится на `internal`, не `ffmpeg_failed` (Gap 3 root cause).
- [ ] Human smoke-test на Windows host: drag-drop работает, extract создаёт .wav, error-копии различимы.
- [ ] Все ранее зелёные 112 тестов остаются зелёными.
</success_criteria>

<output>
Create `.planning/phases/02-media-extraction-pipeline/02-05-SUMMARY.md` when done.

SUMMARY должен содержать:
- Для каждого Gap: что было корневой причиной (фактически найденной в коде, не предположение из VERIFICATION), что изменено, как закрыт regression-guard.
- Diff-list: модифицированные файлы + новые файлы.
- Test results: vitest-counts до/после, ссылка на smoke-output.
- Если human smoke выявил остаточные issues — описать и предложить следующий plan.
</output>
