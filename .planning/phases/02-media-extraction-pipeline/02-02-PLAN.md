---
phase: 02-media-extraction-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - package.json
  - src/main/services/ffmpeg-paths.ts
  - src/main/services/ffmpeg-paths.test.ts
  - src/main/services/progress-parser.ts
  - src/main/services/progress-parser.test.ts
  - src/main/services/media-extractor.ts
  - src/main/services/media-extractor.test.ts
  - src/main/ipc/media.ts
  - src/main/ipc/media.test.ts
  - src/main/utilities/ffmpeg-runner.ts
  - src/main/index.ts
autonomous: true
requirements: [MEDIA-01, MEDIA-03]
tags: [backend, ffmpeg, utility-process, media-extraction, cache, electron]

must_haves:
  truths:
    - "`media-extractor.startExtract(absPath, durationSec)` форкает utilityProcess `ffmpeg-runner.cjs` через `utilityProcess.fork`, шлёт ему `{ type:'start', ffmpegPath, inputPath, outputPath, durationSec }`"
    - "Извлечение пишется в `<userData>/extracted/<hash>.wav.tmp`, переименовывается в `<hash>.wav` только при exit code 0; hash = sha1(absPath:size:mtimeMs)"
    - "Cache-hit: если `<hash>.wav` существует и size>0 — startExtract возвращает Result моментально без fork"
    - "Cancel вызывает `proc.kill()` (SIGTERM), удаляет `.tmp`, возвращает Result `{ok:false, reason:'cancelled'}`"
    - "Прогресс из stdout ffmpeg парсится по `out_time_us=` → `{percent, etaSec}`, шлётся в renderer через `webContents.send(Channels.MEDIA_PROGRESS, {jobId, percent, etaSec})`"
    - "`probe(path)` спавнит ffprobe-бинарник из `@ffprobe-installer/ffprobe` (короткоживущий child_process в main), парсит JSON, возвращает `{durationSec, sizeBytes, name}`"
    - "ipcMain.handle media.* делегирует валидацию `path.isAbsolute(...)`, `endsWith('.mp4')`, `fs.access(R_OK)` ДО вызова сервиса; reason-коды соответствуют D-16"
    - "`ensureExecutable` chmod 0o755 ffmpeg/ffprobe на Linux/macOS вызывается ОДИН раз при app init (в `src/main/index.ts` после `app.whenReady()`)"
    - "ffmpeg args ровно: `-hide_banner -nostats -loglevel error -i <input> -vn -ac 1 -ar 16000 -c:a pcm_s16le -progress pipe:1 -y <output>.tmp`"
  artifacts:
    - path: "src/main/services/ffmpeg-paths.ts"
      provides: "resolveFfmpeg(), resolveFfprobe(), ensureExecutable(p)"
      exports: ["resolveFfmpeg", "resolveFfprobe", "ensureExecutable"]
    - path: "src/main/services/progress-parser.ts"
      provides: "parseProgressLine(line, durationSec, startedMs, lastPct) → ParsedProgress|null — pure function"
      exports: ["parseProgressLine"]
    - path: "src/main/services/media-extractor.ts"
      provides: "MediaExtractor singleton: init(), probe(path), startExtract(path,durationSec), cancel(jobId)"
      exports: ["mediaExtractor"]
    - path: "src/main/utilities/ffmpeg-runner.ts"
      provides: "Полная CJS utility entrypoint: spawn ffmpeg, парсинг stdout, parentPort progress/done/error"
    - path: "src/main/ipc/media.ts"
      provides: "Реальные handlers (заменяют stub Plan 01): pickFile, probe, extractAudio, cancel + webContents.send для progress"
  key_links:
    - from: "src/main/services/media-extractor.ts"
      to: "src/main/utilities/ffmpeg-runner.ts"
      via: "utilityProcess.fork(join(__dirname, 'ffmpeg-runner.cjs'))"
      pattern: "utilityProcess\\.fork"
    - from: "src/main/services/media-extractor.ts"
      to: "Channels.MEDIA_PROGRESS"
      via: "BrowserWindow.getAllWindows()[0]?.webContents.send"
      pattern: "MEDIA_PROGRESS"
    - from: "src/main/ipc/media.ts"
      to: "src/main/services/media-extractor.ts"
      via: "import { mediaExtractor }"
      pattern: "mediaExtractor\\.(probe|startExtract|cancel)"
    - from: "src/main/index.ts"
      to: "mediaExtractor.init() + ensureExecutable"
      via: "после registerIpcHandlers()"
      pattern: "mediaExtractor\\.init"
---

<objective>
Реализовать полный backend-слайс извлечения аудио: 
1) утилиты резолва путей бинарников + chmod fallback; 
2) чистая функция парсинга `-progress pipe:1`; 
3) CJS utility-entrypoint, спавнящий ffmpeg; 
4) singleton-сервис `media-extractor` с job-реестром, кешем и cancel; 
5) реальные IPC handlers media.*; 
6) app-init wiring. 
После этого плана через `npm run test -- --run` можно проверить полный pipeline в dev (с моками utilityProcess); UI (Plan 03) уже сможет дергать реальный сервис.

Purpose: единая когезивная единица — backend, который превращает absolute mp4 path → wav на диске. Разделять `ffmpeg-paths`/`progress-parser`/`media-extractor`/`ffmpeg-runner` по разным планам — scavenger hunt; они тесно сцеплены через типы и контракт IPC payload.
Output: 5 сервисов + полностью реализованные handlers + tested via unit-mocks utilityProcess.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-media-extraction-pipeline/02-CONTEXT.md
@.planning/phases/02-media-extraction-pipeline/02-RESEARCH.md
@.planning/phases/02-media-extraction-pipeline/02-PATTERNS.md
@.planning/phases/02-media-extraction-pipeline/02-VALIDATION.md
@.planning/phases/02-media-extraction-pipeline/02-01-SUMMARY.md
@src/main/services/secrets-store.ts
@src/main/services/secrets-store.test.ts
@src/main/services/secure-backend.ts
@src/main/services/secure-backend.test.ts
@src/main/ipc/settings.ts
@src/main/index.ts
@src/shared/ipc.ts
@tests/setup.ts

<interfaces>
Контракт `media.*` зафиксирован Plan 01 в `src/shared/ipc.ts`. Backend этого плана реализует ровно те типы:
- `MediaReason` (D-16): `'invalid_argument' | 'not_mp4' | 'file_not_found' | 'ffmpeg_failed' | 'cancelled' | 'disk_full' | 'internal'`.
- `MediaProbeResult`, `MediaExtractResult`, `MediaProgressEvent` — см. shared/ipc.ts.
- Job-payload utility'ой (внутренний, не shared): `{ type:'start', ffmpegPath:string, inputPath:string, outputPath:string, durationSec:number }` (main→utility) и `{type:'progress',percent:number,etaSec:number|null}` / `{type:'done',code:number,stderrTail:string}` / `{type:'error',message:string}` (utility→main).
- ffmpeg args (D-01 + RESEARCH §Pattern 3): `['-hide_banner','-nostats','-loglevel','error','-i', inputPath, '-vn','-ac','1','-ar','16000','-c:a','pcm_s16le','-progress','pipe:1','-y', outputPath]`.
- ffprobe args (RESEARCH §Pattern 6): `['-hide_banner','-loglevel','error','-print_format','json','-show_format','-show_streams', inputPath]`.
- cache hash (D-11): `sha1(`${absPath}:${stat.size}:${stat.mtimeMs}`)`, hex.
- userData path (D-11): `join(app.getPath('userData'), 'extracted', `${hash}.wav`)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ffmpeg-paths + progress-parser (чистые утилиты) + установка npm-зависимостей</name>
  <files>package.json, src/main/services/ffmpeg-paths.ts, src/main/services/ffmpeg-paths.test.ts, src/main/services/progress-parser.ts, src/main/services/progress-parser.test.ts</files>
  <read_first>
    - src/main/services/secure-backend.ts (analog для platform-stub + singleton-конвенции)
    - src/main/services/secure-backend.test.ts (analog для stubPlatform helper)
    - .planning/phases/02-media-extraction-pipeline/02-PATTERNS.md (§src/main/services/ffmpeg-paths.ts, §src/main/services/progress-parser.ts — exact code excerpts)
    - .planning/phases/02-media-extraction-pipeline/02-RESEARCH.md (§Pattern 4 — точные правила парсинга прогресса; §Pattern 5 — path resolve; §Package Legitimacy Audit)
    - .planning/phases/02-media-extraction-pipeline/02-CONTEXT.md (D-08, D-11, D-12, D-18)
  </read_first>
  <behavior>
    - `parseProgressLine('out_time_us=5000000', 10, startedMs, -1)` → `{ percent: 50, etaSec: <число> }`.
    - `parseProgressLine('out_time_us=5000000', 10, startedMs, 50)` → `null` (дедуп: pct === lastPct).
    - `parseProgressLine('bitrate=...', 10, ...)` → `null`.
    - `parseProgressLine('out_time_us=5000000', 0, ...)` → `null` (divide-by-zero guard).
    - `percent` cap = 99 даже на out_time_us > durationSec*1e6.
    - `resolveFfmpeg()` возвращает строку, и если входной путь содержит `app.asar` — она заменена на `app.asar.unpacked`.
    - `resolveFfprobe()` — то же поведение для ffprobe.
    - `ensureExecutable(p)` на `process.platform === 'win32'` — no-op (нет вызовов fs.chmod).
    - `ensureExecutable(p)` на 'linux'/'darwin': если `fs.access(p, X_OK)` падает — вызывается `fs.chmod(p, 0o755)`. Если access ок — chmod НЕ вызывается.
  </behavior>
  <action>
    Установить новые зависимости:
    ```
    npm install --save-exact ffmpeg-static@5.3.0 @ffprobe-installer/ffprobe@2.1.2
    ```
    (Версии — [VERIFIED] в RESEARCH §Standard Stack / §Package Legitimacy Audit Approved. Per RESEARCH security domain — не [ASSUMED]/[SUS], blocking checkpoint не требуется.)

    `src/main/services/ffmpeg-paths.ts` — следовать exact паттерну из PATTERNS.md §src/main/services/ffmpeg-paths.ts:
    - imports: `import { promises as fs, constants as fsc } from 'node:fs'`, `import ffmpegStatic from 'ffmpeg-static'`, `import { path as ffprobeStatic } from '@ffprobe-installer/ffprobe'`.
    - `export function resolveFfmpeg(): string` — если `ffmpegStatic == null` throw fail-fast Error; иначе `return ffmpegStatic.replace('app.asar', 'app.asar.unpacked')`.
    - `export function resolveFfprobe(): string` — то же для `ffprobeStatic`.
    - `export async function ensureExecutable(p: string): Promise<void>` — early return на `process.platform === 'win32'`; try `fs.access(p, fsc.X_OK)`; catch → `fs.chmod(p, 0o755)`. JSDoc — ссылка на CONTEXT D-18 и RESEARCH Pitfall #2.

    `src/main/services/ffmpeg-paths.test.ts` — analog `secure-backend.test.ts` с `stubPlatform`. Тест-кейсы:
    1) `resolveFfmpeg()` на синтетическом пути, содержащем `app.asar` — заменяет на `app.asar.unpacked` (мок `vi.mock('ffmpeg-static', () => ({ default: '/some/path/app.asar/node_modules/ffmpeg-static/ffmpeg' }))`).
    2) `resolveFfprobe()` симметрично.
    3) `ensureExecutable` на win32: `fs.chmod` НЕ вызван.
    4) `ensureExecutable` на linux + access reject: `fs.chmod` вызван с `(p, 0o755)`.
    5) `ensureExecutable` на linux + access success: `fs.chmod` НЕ вызван.

    `src/main/services/progress-parser.ts` — exact из PATTERNS.md §src/main/services/progress-parser.ts:
    - Экспорт `interface ParsedProgress { percent: number; etaSec: number | null }`.
    - Экспорт `function parseProgressLine(line: string, durationSec: number, startedMs: number, lastPct: number): ParsedProgress | null`.
    - Тело: парсит `key=value`, фильтрует key === 'out_time_us', вычисляет `pct = min(99, floor(us/1e6/durationSec*100))`, dedup `if (pct === lastPct) return null`, `elapsed = (Date.now() - startedMs)/1000`, `eta = pct>0 ? max(0, round((100/pct - 1)*elapsed)) : null`.
    - НИКАКИХ side-effects, никаких импортов кроме встроенных типов.

    `src/main/services/progress-parser.test.ts` — таблица it.each по поведенческим case-ам выше. Структура describe/it — по `secure-backend.test.ts`.
  </action>
  <verify>
    <automated>npm run test -- --run src/main/services/progress-parser.test.ts src/main/services/ffmpeg-paths.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Оба test-файла зелёные.
    - `grep -c "ffmpeg-static@5.3.0" package.json` >= 1 (через `"ffmpeg-static": "5.3.0"` точное соответствие через `--save-exact`).
    - `grep -c "@ffprobe-installer/ffprobe" package.json` >= 1.
    - `grep -c "app.asar.unpacked" src/main/services/ffmpeg-paths.ts` >= 1.
    - `grep -c "out_time_us" src/main/services/progress-parser.ts` >= 1.
    - `grep -v '^[[:space:]]*//' src/main/services/progress-parser.ts | grep -cE "^import.*(electron|node:fs|child_process)" == 0` (pure function — никаких side-effect импортов).
  </acceptance_criteria>
  <done>Чистые утилиты резолва путей + парсера прогресса покрыты тестами; npm-зависимости установлены.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: media-extractor (singleton) + ffmpeg-runner (CJS utility) + media-extractor.test.ts</name>
  <files>src/main/services/media-extractor.ts, src/main/services/media-extractor.test.ts, src/main/utilities/ffmpeg-runner.ts, src/main/index.ts</files>
  <read_first>
    - src/main/services/secrets-store.ts (целиком — singleton-конвенция, init, error-маппинг ENOENT)
    - src/main/services/secrets-store.test.ts (целиком — структура `freshStore()`, tmpdir USER_DATA, `vi.resetModules()`)
    - src/main/index.ts (целиком — порядок `app.whenReady` → registerIpcHandlers; добавим init)
    - tests/setup.ts (целиком — `utilityProcess.fork.__emit` helper)
    - .planning/phases/02-media-extraction-pipeline/02-RESEARCH.md (§Pattern 3 — exact lifecycle, §Pattern 6 — ffprobe, §Security Domain — path traversal mitigation)
    - .planning/phases/02-media-extraction-pipeline/02-PATTERNS.md (§src/main/services/media-extractor.ts, §src/main/utilities/ffmpeg-runner.ts, §src/main/services/media-extractor.test.ts)
    - .planning/phases/02-media-extraction-pipeline/02-CONTEXT.md (D-09, D-10, D-11, D-12, D-16, D-17)
  </read_first>
  <behavior>
    - `mediaExtractor.init()` создаёт `<userData>/extracted/` через `fs.mkdir({recursive:true})` и вызывает `ensureExecutable(resolveFfmpeg())` + `ensureExecutable(resolveFfprobe())` ровно один раз.
    - `mediaExtractor.probe(absPath)` спавнит ffprobe через `child_process.spawn(resolveFfprobe(), ['-hide_banner','-loglevel','error','-print_format','json','-show_format','-show_streams', absPath])`, собирает stdout, парсит JSON, возвращает `Result<{durationSec, sizeBytes, name}>`. На ENOENT → `reason:'file_not_found'`. На exit code != 0 → `'ffmpeg_failed'`.
    - `mediaExtractor.startExtract(absPath, durationSec)`:
      a) hash = `sha1(`${absPath}:${stat.size}:${stat.mtimeMs}`)`, hex.
      b) outPath = `join(cacheDir, hash + '.wav')`. Если `fs.stat(outPath).size > 0` — RESOLVE `{ok:true, data:{jobId, audioPath:outPath}}` БЕЗ fork (cache-hit, D-12).
      c) Иначе `utilityProcess.fork(join(__dirname, 'ffmpeg-runner.cjs'), [], { serviceName:'scrubber-ffmpeg', stdio:'pipe' })`.
      d) на event 'spawn' шлёт `proc.postMessage({type:'start', ffmpegPath:resolveFfmpeg(), inputPath:absPath, outputPath: outPath + '.tmp', durationSec})`.
      e) на event 'message' с `type==='progress'` — вызывает `BrowserWindow.getAllWindows()[0]?.webContents.send(Channels.MEDIA_PROGRESS, {jobId, percent, etaSec})`.
      f) на event 'exit' с `code===0` — `fs.rename(tmp, final)` + resolve `{ok:true, data:{jobId, audioPath:final}}`.
      g) `code !== 0` + не cancelled → `fs.unlink(tmp)` + resolve `{ok:false, reason:'ffmpeg_failed'}`.
      h) `cancelled === true` → `fs.unlink(tmp)` + resolve `{ok:false, reason:'cancelled'}`.
    - `mediaExtractor.cancel(jobId)`: если jobId нет в Map → `{ok:false, reason:'invalid_argument'}`. Иначе `handle.cancelled=true; handle.proc.kill(); return {ok:true}`.
    - `ffmpeg-runner.cjs`: только CJS (`require`), никаких импортов из `src/main/*` или `electron`. Слушает `process.parentPort.on('message')`, на `type==='start'` спавнит `child_process.spawn(ffmpegPath, args, {windowsHide:true})`. stdout → line-buffer → парсит локально (дубль `parseProgressLine` ~10 строк inline) → шлёт `parentPort.postMessage({type:'progress', percent, etaSec})`. stderr → накопить последние 2000 байт → на exit отправить `{type:'done', code, stderrTail}` и `process.exit(code ?? 1)`. `process.on('SIGTERM', () => child?.kill('SIGTERM'))`.
    - `src/main/index.ts`: после `registerIpcHandlers()` добавить `await mediaExtractor.init()` в том же `app.whenReady()` ветке.
  </behavior>
  <action>
    `src/main/services/media-extractor.ts`:
    - Class `MediaExtractor` со state: `jobs: Map<string, JobHandle>`, `cacheDir: string`, `initialised: boolean`.
    - `JobHandle = { jobId, proc, resolve, outPath, cancelled: boolean }`.
    - Singleton: `export const mediaExtractor = new MediaExtractor()` (per Pattern C).
    - `init()`: idempotent, выставляет `cacheDir = join(app.getPath('userData'),'extracted')`, `fs.mkdir({recursive:true})`, вызывает `ensureExecutable(resolveFfmpeg())` и `ensureExecutable(resolveFfprobe())`.
    - `probe(absPath)`: `child_process.spawn` ffprobe, парс JSON `format.duration` (string→Number), `format.size`, `path.basename(absPath)`. Лог префикс `[services/media-extractor]`. ENOENT/EACCES → reason-маппинг.
    - `startExtract(absPath, durationSec)`: см. behavior пункты a-h. Security mitigation (T-02-02-04): `if (!path.isAbsolute(absPath)) return {ok:false, reason:'invalid_argument'}`. ffmpeg args inline — точный массив из §interfaces. ВСЕГДА outputPath под нашим контролем (`<hash>.wav.tmp`).
    - `cancel(jobId)`: см. behavior.
    - Лог префикс `[services/media-extractor]`. Никаких `throw` через границу IPC — всё через Result.

    `src/main/services/media-extractor.test.ts` — analog `secrets-store.test.ts`:
    - `const USER_DATA = join(os.tmpdir(), 'scrubber-test', 'userData')`.
    - `beforeEach`: rm -rf USER_DATA, `vi.resetModules()`, переинициализировать mock `utilityProcess.fork` (clear).
    - `freshExtractor()` factory: import свежий media-extractor, `await mediaExtractor.init()`.
    - Test-кейсы (через `(electron.utilityProcess.fork as vi.Mock).mock.results[0].value.__emit('spawn'|'message'|'exit', ...)`):
      1) **happy path**: startExtract → fork called с правильным path → emit 'spawn' → проверить `postMessage({type:'start', ...})` payload содержит правильные args → emit 'message' `{type:'progress', percent:50, etaSec:5}` → проверить `webContents.send` вызван с `Channels.MEDIA_PROGRESS`. Создать вручную `.tmp` файл перед emit 'exit', emit 'exit' code 0 → rename вызван, resolve `{ok:true, data:{audioPath}}`.
      2) **cancel**: startExtract → дождаться fork → `mediaExtractor.cancel(jobId)` → проверить `proc.kill()` вызван → emit 'exit' code != 0 → resolve `{ok:false, reason:'cancelled'}`, `.tmp` файл удалён (или unlink был вызван).
      3) **ffmpeg_failed**: startExtract → emit 'exit' code 1 → resolve `{ok:false, reason:'ffmpeg_failed'}`.
      4) **cache hit**: до startExtract создать `<hash>.wav` (>0 байт) → startExtract resolve мгновенно `{ok:true}`, `utilityProcess.fork` НЕ вызван (mock-call-count === 0).
      5) **invalid path**: `startExtract('relative/path.mp4', 5)` → `{ok:false, reason:'invalid_argument'}`.

    `src/main/utilities/ffmpeg-runner.ts` — заменить placeholder Plan 01 на полную реализацию из RESEARCH §Pattern 3 runner-блок. CJS-стиль (`require`, не `import`), eslint-disable где нужно. Inline-функция парсинга прогресса (~10 строк) — дублирование `parseProgressLine` сознательное (utility-script не должен импортировать из `src/main/services/`). Комментарий вверху: «Inline-парсер прогресса — дубль services/progress-parser.ts; покрыт тестом отдельно. См. 02-PATTERNS.md §src/main/utilities/ffmpeg-runner.ts».

    `src/main/index.ts`: добавить `import { mediaExtractor } from './services/media-extractor'`. После `registerIpcHandlers()` вызвать `await mediaExtractor.init()`. Лог префикс `[main]`.
  </action>
  <verify>
    <automated>npm run test -- --run src/main/services/media-extractor.test.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `media-extractor.test.ts` зелёный, минимум 5 тест-кейсов (happy, cancel, ffmpeg_failed, cache-hit, invalid).
    - `grep -c "utilityProcess.fork" src/main/services/media-extractor.ts` >= 1.
    - `grep -c "Channels.MEDIA_PROGRESS" src/main/services/media-extractor.ts` >= 1.
    - `grep -c "webContents.send" src/main/services/media-extractor.ts` >= 1.
    - `grep -c "require(" src/main/utilities/ffmpeg-runner.ts` >= 1 (CJS-стиль).
    - `grep -v '^[[:space:]]*//' src/main/utilities/ffmpeg-runner.ts | grep -cE "^import " == 0` (нет ESM-импортов вне комментариев).
    - `grep -c "mediaExtractor.init" src/main/index.ts` >= 1.
    - `npm run build` зелёный (out/main/ffmpeg-runner.cjs собран с реальным содержимым; `wc -l out/main/ffmpeg-runner.cjs` > 20).
  </acceptance_criteria>
  <done>Singleton-сервис media-extractor собирает и оркестрирует utility-процесс; cancel/cache/progress работают; тесты покрывают 5 сценариев.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: IPC handlers media.* с валидацией + media.test.ts (заменяет stub Plan 01)</name>
  <files>src/main/ipc/media.ts, src/main/ipc/media.test.ts</files>
  <read_first>
    - src/main/ipc/media.ts (stub из Plan 01 — переписываем)
    - src/main/ipc/settings.ts (целиком — analog для try/catch/Result + invalid_argument валидация)
    - .planning/phases/02-media-extraction-pipeline/02-PATTERNS.md (§src/main/ipc/media.ts — exact паттерны handler-skeleton + defence-in-depth D-06)
    - .planning/phases/02-media-extraction-pipeline/02-RESEARCH.md (§Pattern 1 — pickFile handler точный shape; §Security Domain V5 input validation)
    - .planning/phases/02-media-extraction-pipeline/02-CONTEXT.md (D-04, D-06, D-14, D-16)
  </read_first>
  <behavior>
    - `Channels.MEDIA_PICK_FILE` handler: получает `BrowserWindow.fromWebContents(event.sender)`. Если null → `{ok:false, reason:'internal'}`. Иначе `dialog.showOpenDialog(win, {properties:['openFile'], filters:[{name:'MP4', extensions:['mp4']}]})`. `canceled` или пустой filePaths → `{ok:true, data:null}`. Иначе `{ok:true, data:{path:filePaths[0]}}`.
    - `Channels.MEDIA_PROBE` handler: `typeof args[0] !== 'string'` → `invalid_argument`; `!path.isAbsolute(arg)` → `invalid_argument`; `!arg.toLowerCase().endsWith('.mp4')` → `not_mp4`; `fs.access(arg, R_OK)` reject → `file_not_found`. Иначе делегирует `mediaExtractor.probe(arg)`.
    - `Channels.MEDIA_EXTRACT` handler: те же четыре проверки + дополнительно делает probe для durationSec (или принимает payload `{path, durationSec}` — окончательно выбрать форму в задаче), затем `mediaExtractor.startExtract(...)`. **Решение по форме payload**: payload = `string` (только path); handler сам делает `probe` и берёт `durationSec` из результата — это упрощает renderer (один meta-call probe + один extract-call). Это согласуется с UI-SPEC State Map (selected→extracting без повторного probe).
    - `Channels.MEDIA_CANCEL` handler: `typeof args[0] !== 'string'` → `invalid_argument`; UUID validation (regex `/^[0-9a-f-]{36}$/`) → `invalid_argument`; иначе `mediaExtractor.cancel(jobId)`.
    - Все handlers — Result-тип, лог `[ipc/media]`, никаких throw через IPC.
  </behavior>
  <action>
    `src/main/ipc/media.ts` — переписать stub Plan 01 на полную реализацию. Структура per analog settings.ts:
    - imports: `ipcMain, BrowserWindow, dialog` из 'electron'; `Channels, type Result, type MediaProbeResult, type MediaExtractResult` из '../../shared/ipc'; `mediaExtractor` из '../services/media-extractor'; `promises as fs, constants as fsc` из 'node:fs'; `isAbsolute` из 'node:path'.
    - `export function registerMediaHandlers(): void` — 4 `ipcMain.handle` блока.
    - `MEDIA_PICK_FILE`: signature `async (event, ..._args): Promise<Result<{path:string}|null>>` — НЕ забыть `event` (нужен `BrowserWindow.fromWebContents`). См. Pattern 1 в RESEARCH.
    - `MEDIA_PROBE`: validation chain → `mediaExtractor.probe(args[0] as string)`.
    - `MEDIA_EXTRACT`: validation chain → probe (если probe.ok=false → return reason); → `mediaExtractor.startExtract(path, probe.data.durationSec)`.
    - `MEDIA_CANCEL`: validation chain → `mediaExtractor.cancel(jobId)`.
    - Хелпер `validateMp4Path(arg: unknown): { ok:true, path:string } | { ok:false, reason: MediaReason }` для дедупа D-06 валидации между PROBE/EXTRACT.

    `src/main/ipc/media.test.ts` — analog `secure-backend.test.ts`:
    - `beforeEach`: `vi.resetModules()`, `(electron.ipcMain.handle as vi.Mock).mockClear()`.
    - Тесты:
      1) `registerMediaHandlers()` вызывает `ipcMain.handle` ровно 4 раза с `Channels.MEDIA_PICK_FILE/PROBE/EXTRACT/CANCEL` (snapshot of `mock.calls[i][0]`).
      2) `pickFile`: с моком `dialog.showOpenDialog` возвращает `{canceled:false, filePaths:['C:\\\\test.mp4']}` → handler возвращает `{ok:true, data:{path:'C:\\\\test.mp4'}}`. Filter `[{name:'MP4', extensions:['mp4']}]` проверяется через `expect(dialog.showOpenDialog).toHaveBeenCalledWith(expect.anything(), expect.objectContaining({filters:[{name:'MP4', extensions:['mp4']}]}))`.
      3) `probe`: invalid_argument для non-string, non-absolute, .txt; not_mp4 для `.MP4` ОК (case-insensitive).
      4) `extract`: пробивает defence-in-depth (`relative.mp4` → invalid_argument; `/abs/file.txt` → not_mp4).
      5) `cancel`: invalid UUID → invalid_argument; валидный UUID делегирует `mediaExtractor.cancel`.
  </action>
  <verify>
    <automated>npm run test -- --run src/main/ipc/media.test.ts</automated>
    <automated>npm run test -- --run</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/ipc/media.test.ts` зелёный, >=5 it-блоков.
    - Полный `npm run test -- --run` зелёный (включая secrets-store, secure-backend, preload.test.ts, ipc-tests, services-tests из этой фазы).
    - `grep -c "ipcMain.handle" src/main/ipc/media.ts` == 4.
    - `grep -c "endsWith('.mp4')" src/main/ipc/media.ts` >= 1.
    - `grep -c "isAbsolute" src/main/ipc/media.ts` >= 1.
    - `grep -c "'media:" src/main/ipc/media.ts` == 0 (нет сырых литералов).
  </acceptance_criteria>
  <done>media IPC handlers полностью реализованы с defence-in-depth валидацией; вся фаза backend готова для UI Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→main IPC | `path: string` приходит из renderer (drop или dialog filtered). Main — авторитет валидации (D-06). |
| main→utilityProcess | Main передаёт `{ffmpegPath, inputPath, outputPath, durationSec}` через `parentPort.postMessage`. Все три пути под нашим контролем (ffmpegPath из resolveFfmpeg; outputPath = `<hash>.wav.tmp` нами составлен; inputPath валидирован). |
| utilityProcess→ffmpeg | `spawn(ffmpegPath, args)` без shell. Argument injection невозможна напрямую, но `inputPath` может начинаться с `-` если злонамеренный mp4. |
| main→filesystem userData | wav пишется только в `<userData>/extracted/<hash>.wav` — фиксированный prefix, hash детерминирован. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | media.extractAudio path traversal | mitigate | `isAbsolute(path)` гейт в `src/main/ipc/media.ts` validateMp4Path; outputPath ВСЕГДА `<userData>/extracted/<hash>.wav` — renderer не контролирует output. |
| T-02-02-02 | Tampering | argument injection через имя файла начинающееся с `-` | mitigate | `spawn(ffmpegPath, args)` БЕЗ shell. inputPath нормализуется через `path.resolve(input)` ДО передачи в args (test проверяет нормализацию). Если RESEARCH рекомендует `--` separator — добавить в args перед input (`['-i', '--', inputPath, ...]` не работает с ffmpeg; используем `path.resolve` + проверку `!input.startsWith('-')` после resolve). |
| T-02-02-03 | Information disclosure | stderr ffmpeg утечка в renderer | mitigate | stderr НЕ возвращается в Result. На `ffmpeg_failed` логируется только в `console.error('[services/media-extractor]')`. Renderer видит только `reason:'ffmpeg_failed'`. |
| T-02-02-04 | DoS | бесконечный/злой mp4 → ffmpeg висит | mitigate | Cancel-кнопка (D-09) + один job за раз (D-10). Реализовано в startExtract/cancel + media-extractor.test.ts. |
| T-02-02-05 | Supply chain | ffmpeg-static / @ffprobe-installer | accept | Оба пакета [VERIFIED] в RESEARCH §Package Legitimacy Audit (Approved); ffmpeg-static уже в CLAUDE.md §Recommended Stack. Установка через `--save-exact` фиксирует версию 5.3.0 / 2.1.2. |
| T-02-02-06 | Tampering | symlink-attack (input = symlink) | accept | ffmpeg прочтёт байты как медиа, упадёт `ffmpeg_failed`. RESEARCH §Security Domain отмечает «не критично». |
| T-02-02-07 | Information disclosure | path в `audioPath` Result | accept | `audioPath` в userData — by design (Phase 3 будет его читать). Не secret. |
| T-02-02-SC | Tampering | npm-installs (ffmpeg-static, @ffprobe-installer) | mitigate | RESEARCH §Package Legitimacy Audit Approved [VERIFIED via CLAUDE.md + npm registry]. Blocking checkpoint не требуется. |
</threat_model>

<verification>
- Vitest полный (`npm run test -- --run`) зелёный — все unit покрытия для backend.
- `npm run typecheck` зелёный.
- `npm run build` собирает `out/main/ffmpeg-runner.cjs` >20 строк (реальная логика).
- Все four `ipcMain.handle` зарегистрированы; validateMp4Path защищает PROBE и EXTRACT.
- Не существует `'media:...'` строкового литерала вне `src/shared/ipc.ts`.
</verification>

<success_criteria>
- В dev (через `npm run dev`) main-процесс инициализируется без ошибок; `mediaExtractor.init()` создаёт `<userData>/extracted/` и применяет chmod (на Linux/macOS).
- Юнит-тесты покрывают: парсинг прогресса, path resolve, ensureExecutable, happy/cancel/failed/cache-hit/invalid сценарии extractor'а, валидацию IPC handlers.
- Plan 03 (UI) может вызывать `window.scrubber.media.pickFile/probe/extractAudio/cancel/onProgress` и получать реальный результат.
</success_criteria>

<output>
Создать `.planning/phases/02-media-extraction-pipeline/02-02-SUMMARY.md` со списком файлов, ссылками на CONTEXT decisions (D-08..D-17), и явным фиксированием:
- Точная форма payload `MEDIA_EXTRACT` (только `path: string`; durationSec вычисляется handler'ом через probe).
- Выбор inline-парсера прогресса в ffmpeg-runner.cjs (дубль `parseProgressLine`) и оправдание.
- Замечания по A6 (BrowserWindow.getAllWindows()[0]) — пометка «один window в v1; refactor если v2 multi-window».
</output>
