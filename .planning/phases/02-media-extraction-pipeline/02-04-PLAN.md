---
phase: 02-media-extraction-pipeline
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - tests/integration/extract-real.test.ts
  - tests/fixtures/media/generate.mjs
  - .gitignore
  - vitest.config.ts
  - .planning/phases/02-media-extraction-pipeline/02-VERIFICATION.md
autonomous: false
requirements: [MEDIA-03, MEDIA-04]
tags: [integration, packaging, smoke, verification, ffmpeg, asar-unpack]

must_haves:
  truths:
    - "Интеграционный тест `tests/integration/extract-real.test.ts` использует реальный ffmpeg-static (не mocked) и подтверждает: probe возвращает корректную durationSec для сгенерированного 5-сек mp4; startExtract производит wav 16kHz mono PCM s16le на диске"
    - "Скрипт `tests/fixtures/media/generate.mjs` создаёт фикстуры (`short.mp4`, `no-audio.mp4`) через ffmpeg-static; запускается через `node tests/fixtures/media/generate.mjs`"
    - "Фикстуры в `.gitignore` (не коммитятся; генерируются)"
    - "Упакованная сборка (`npm run build` + `electron-builder --dir`) на Windows host: drop sample.mp4 → wav в `<userData>/extracted/` (manual smoke)"
    - "02-VERIFICATION.md документирует Windows smoke результат + явные SKIP-метки для Linux/macOS"
    - "ffmpeg-static и @ffprobe-installer корректно распакованы в `app.asar.unpacked/` в packaged-build (smoke это подтверждает)"
  artifacts:
    - path: "tests/integration/extract-real.test.ts"
      provides: "Integration test реального pipeline через ffmpeg-static; покрывает MEDIA-03 end-to-end в dev"
    - path: "tests/fixtures/media/generate.mjs"
      provides: "Standalone fixture generator (короткий mp4 с аудио + mp4 без аудио)"
    - path: ".planning/phases/02-media-extraction-pipeline/02-VERIFICATION.md"
      provides: "Чек-лист packaged smoke для всех трёх ОС, заполненный для Windows host"
  key_links:
    - from: "tests/integration/extract-real.test.ts"
      to: "src/main/services/media-extractor.ts"
      via: "real ffmpeg-static (no mock)"
      pattern: "mediaExtractor\\.(probe|startExtract)"
    - from: "02-VERIFICATION.md"
      to: "MEDIA-04 acceptance"
      via: "manual smoke результаты"
      pattern: "Windows packaged smoke"
---

<objective>
Закрыть acceptance Phase 2 двумя путями:
1) **Integration test** — реальный ffmpeg-static прогоняется через media-extractor pipeline на 5-сек сгенерированном mp4; покрывает A3/A4 assumptions из RESEARCH (asarUnpack путь в dev + `-progress pipe:1` + WAV-формат). Запускается в `npm run test`.
2) **Packaged smoke** на Windows host (D-19) — упакованная сборка фактически работает. Linux/macOS — best-effort SKIP, фиксируется в VERIFICATION.

После Plan 04 фаза готова к `/gsd:verify-work`.

Purpose: закрыть последний gap (A3 — `asarUnpack` действительно копирует бинарники + ffmpeg исполняется из `app.asar.unpacked`) единственным способом, которым его можно закрыть — фактическим packaged-запуском.
Output: VERIFICATION.md с PASS/SKIP метками, integration test зелёный.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-media-extraction-pipeline/02-CONTEXT.md
@.planning/phases/02-media-extraction-pipeline/02-RESEARCH.md
@.planning/phases/02-media-extraction-pipeline/02-PATTERNS.md
@.planning/phases/02-media-extraction-pipeline/02-VALIDATION.md
@.planning/phases/02-media-extraction-pipeline/02-01-SUMMARY.md
@.planning/phases/02-media-extraction-pipeline/02-02-SUMMARY.md
@.planning/phases/02-media-extraction-pipeline/02-03-SUMMARY.md
@.planning/phases/01-foundation-app-shell/VERIFICATION.md
@tests/integration/secrets-persist.test.ts
@vitest.config.ts
@electron-builder.yml

<interfaces>
- `mediaExtractor.probe(absPath): Promise<Result<{durationSec, sizeBytes, name}>>` — реальный вызов с реальным ffprobe.
- `mediaExtractor.startExtract(absPath, durationSec): Promise<Result<{jobId, audioPath}>>` — реальный вызов с реальным ffmpeg в utilityProcess.
- Генератор фикстур (RESEARCH §Environment Availability):
  ```
  ffmpeg -f lavfi -i sine=frequency=440:duration=5 \
         -f lavfi -i color=c=blue:s=320x240:d=5 \
         -shortest -y tests/fixtures/media/short.mp4
  ```
- Validation выхода (RESEARCH §Wave 0 Gaps): `ffprobe -print_format json -show_streams <wav>` → `streams[0].codec_name === 'pcm_s16le'`, `sample_rate === '16000'`, `channels === 1`.
- Packaged build команда: `npm run build && npm run package -- --dir` (где `package` — это `electron-builder --dir` или эквивалент; проверить `package.json` scripts; если отсутствует — добавить `"package": "electron-builder --dir"` в `package.json` scripts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fixture generator + integration test с реальным ffmpeg-static</name>
  <files>tests/fixtures/media/generate.mjs, tests/integration/extract-real.test.ts, .gitignore, vitest.config.ts</files>
  <read_first>
    - tests/integration/secrets-persist.test.ts (целиком — analog: tmpdir-изоляция, real-deps, vi.unmock pattern)
    - vitest.config.ts (целиком — расширить include для tests/integration; testTimeout для длинных тестов)
    - .gitignore (текущий список — добавить fixture-папку с mp4)
    - .planning/phases/02-media-extraction-pipeline/02-RESEARCH.md (§Environment Availability — точная ffmpeg-командаdla генерации; §Wave 0 Gaps — структура integration теста; §Validation Architecture sampling)
    - .planning/phases/02-media-extraction-pipeline/02-CONTEXT.md (D-19 — acceptance gate)
    - src/main/services/media-extractor.ts (Plan 02 — реальный сервис под тестом)
  </read_first>
  <behavior>
    - `node tests/fixtures/media/generate.mjs` создаёт:
      a) `tests/fixtures/media/short.mp4` (5 сек, 440Hz sine wave audio + синий video 320x240).
      b) `tests/fixtures/media/no-audio.mp4` (5 сек, видео только, `-an`).
      Если файлы уже существуют — не пересоздавать (idempotent).
    - `tests/integration/extract-real.test.ts`:
      a) `beforeAll`: запустить generate.mjs если фикстур нет.
      b) `it('probe returns ~5 second duration')`: `mediaExtractor.probe(absPath of short.mp4)` → `result.ok === true`, `result.data.durationSec` в диапазоне 4.5..5.5.
      c) `it('extracts wav PCM 16kHz mono')`: `startExtract(short.mp4, 5)` → `result.ok===true`, `result.data.audioPath` существует и >0 байт; ffprobe over audioPath показывает `codec_name === 'pcm_s16le'`, `sample_rate === '16000'`, `channels === 1`.
      d) `it('cache-hit on second call')`: повторный `startExtract` того же файла резолвится без spawn (можно проверить через timing < 100ms или через snapshot времени).
      e) `it('handles no-audio mp4 gracefully')`: `startExtract(no-audio.mp4, 5)` либо успешно создаёт wav 0-amplitude, либо возвращает `ffmpeg_failed` — оба варианта приемлемы; тест проверяет что Result типа без exception.
    - Timeout test'а: 60 секунд (RESEARCH §Wave 0 Gaps).
    - Test НЕ мокает electron — использует `vi.unmock('electron')` либо отдельный config-флаг.
  </behavior>
  <action>
    `tests/fixtures/media/generate.mjs`:
    - ESM-stylefile (`.mjs`).
    - `import { execFile } from 'node:child_process'`, `import { promisify } from 'node:util'`, `import ffmpegStatic from 'ffmpeg-static'`, `import { existsSync, mkdirSync } from 'node:fs'`, `import { resolve, dirname, fileURLToPath } from 'node:path'`.
    - Цели: `short.mp4` (lavfi sine 440 + color blue 320x240, -shortest, 5 сек) и `no-audio.mp4` (только color, `-an`, 5 сек).
    - Idempotent (skip if exists).
    - Логи через `console.log('[fixtures]', ...)`.

    `tests/integration/extract-real.test.ts`:
    - Заголовок: `// @vitest-environment node` (НЕ jsdom).
    - В начале файла: `import { vi } from 'vitest'; vi.unmock('electron')` — НЕ работает если глобальный mock через `vi.mock` в setup. **Альтернатива:** условный mock в `tests/setup.ts` через env var `process.env.INTEGRATION === '1'` — НЕТ; лучше отдельный vitest config.
    - **Финальное решение:** добавить в `vitest.config.ts` поле `test.exclude: ['tests/integration/**']` для default-run; создать ОТДЕЛЬНЫЙ `vitest.integration.config.ts`, который НЕ загружает `tests/setup.ts` глобальный mock и включает только `tests/integration/**`; добавить в `package.json` script `"test:integration": "vitest run --config vitest.integration.config.ts"`. Альтернатива (проще): в `tests/setup.ts` обернуть `vi.mock('electron')` в `if (!process.env.VITEST_INTEGRATION)`. **Выбираем альтернативу 2** (проще, меньше файлов) — добавить ENV-флаг в setup.ts и script `"test:integration": "VITEST_INTEGRATION=1 vitest run tests/integration"` (cross-env через ` $env:VITEST_INTEGRATION='1'; ...` на Windows? — использовать пакет `cross-env` либо `npx cross-env` или нативный setup).

    **Минимальное решение для Windows host:** в `package.json` scripts добавить `"test:integration": "node -e \"process.env.VITEST_INTEGRATION='1'\" && vitest run tests/integration"` — **НЕ работает** (node -e и vitest run — разные процессы). Корректно через `cross-env`:
    ```
    npm install --save-exact --save-dev cross-env@7.0.3
    ```
    Script: `"test:integration": "cross-env VITEST_INTEGRATION=1 vitest run tests/integration"`.

    `tests/setup.ts`: обернуть существующий `vi.mock('electron', ...)` блок:
    ```
    if (!process.env.VITEST_INTEGRATION) {
      vi.mock('electron', () => { /* ... existing mock ... */ })
    }
    ```
    Так integration-runs не мокают electron, regular runs — мокают.

    Тест-кейсы — см. behavior. Использовать реальный `app` import из 'electron' — НО: integration-test работает в node-режиме без Electron-рантайма. `app.getPath('userData')` без Electron-рантайма не сработает. **Решение:** в integration-тесте импортировать `media-extractor` после mock `app.getPath` через `vi.spyOn` ИЛИ создать узкий мок только для `app.getPath` (всё остальное реальное). Самое простое: в setup.ts оставить mock для `app` (это минимальный shim, не нужен real Electron), но снять mock с `child_process`/`utilityProcess`. **Финально:** в integration setup мокать только `app` (для getPath/whenReady) + `BrowserWindow` (для webContents.send в media-extractor) + `dialog`; `utilityProcess.fork` оставить РЕАЛЬНЫМ. Но `utilityProcess` доступен только в Electron-рантайме, не в node — это **блокирующая проблема**.

    **Корректное финальное решение:** integration test НЕ зовёт `mediaExtractor.startExtract` (тот требует `utilityProcess.fork`, которого нет в node-vitest). Вместо этого:
    - Тестируем `mediaExtractor.probe` напрямую (использует `child_process.spawn` — доступно в node).
    - Тестируем `parseProgressLine` против реального ffmpeg stdout (захватываем stdout `child_process.spawn(ffmpegPath, [..., '-progress', 'pipe:1', ...])` отдельно и парсим).
    - Полный pipeline через `utilityProcess.fork` — покрывается только **packaged smoke** (Task 2 — manual).
    Это сужает integration test до probe+ffmpeg-CLI, но это всё равно реальный ffmpeg-static и MEDIA-03 покрыт частично автоматически.

    Тест-кейсы пересмотренные:
    a) `it('ffmpeg-static binary exists and is executable')`: `resolveFfmpeg()` + `fs.access(p, X_OK)` — успешно (или после `ensureExecutable`).
    b) `it('ffprobe returns ~5 second duration for short.mp4')`: прямой `child_process.spawn(resolveFfprobe(), [...args])`, парсинг JSON, durationSec ≈ 5.
    c) `it('ffmpeg produces 16kHz mono PCM s16le WAV')`: прямой `child_process.spawn(resolveFfmpeg(), exact args из contract)` против `short.mp4` → wav на диске; ffprobe wav-а показывает корректные параметры.
    d) `it('parseProgressLine handles real ffmpeg stdout line')`: захватить hard-coded строку `out_time_us=2500000`, проверить parseProgressLine(line, 5, Date.now()-1000, -1) → percent=50.

    Это полностью покрывается через `child_process` без `utilityProcess`. Acceptance MEDIA-03 для real pipeline — Task 2 packaged smoke.

    `.gitignore` — добавить:
    ```
    tests/fixtures/media/*.mp4
    tests/fixtures/media/*.wav
    ```

    `vitest.config.ts`: добавить `test.testTimeout: 60000` для длинных тестов (или per-file `testTimeout` через `describe.concurrent` опции).
  </action>
  <verify>
    <automated>node tests/fixtures/media/generate.mjs</automated>
    <automated>npm run test:integration</automated>
    <automated>npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/media/short.mp4` существует после `node tests/fixtures/media/generate.mjs`.
    - `tests/fixtures/media/short.mp4` НЕ tracked в git (`git check-ignore tests/fixtures/media/short.mp4` возвращает успех).
    - `npm run test:integration` зелёный, >=4 it-блоков.
    - `npm run test -- --run` (regular suite) остаётся зелёным.
    - `grep -c "VITEST_INTEGRATION" tests/setup.ts` >= 1.
  </acceptance_criteria>
  <done>Integration test покрывает MEDIA-03 end-to-end через реальный ffmpeg/ffprobe (без utilityProcess); fixture-генератор работает.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Packaged build smoke на Windows host + заполнение VERIFICATION.md</name>
  <files>.planning/phases/02-media-extraction-pipeline/02-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/01-foundation-app-shell/VERIFICATION.md (analog VERIFICATION шаблон)
    - .planning/phases/02-media-extraction-pipeline/02-CONTEXT.md (D-19 — acceptance gate)
    - .planning/phases/02-media-extraction-pipeline/02-RESEARCH.md (§Pitfalls #1, #2, #3, #4; §Validation Architecture sampling)
    - electron-builder.yml (Plan 01 расширил asarUnpack)
    - package.json (проверить наличие script для packaged build)
  </read_first>
  <what-built>
    - Plan 01 расширил `electron-builder.yml` `asarUnpack` под ffmpeg-static и @ffprobe-installer.
    - Plan 02 реализовал `media-extractor` + `ffmpeg-paths` с `.replace('app.asar','app.asar.unpacked')` и `ensureExecutable` (chmod fallback).
    - Plan 03 реализовал Transcribe UI; drop работает в dev.
    - Сейчас нужно подтвердить, что упакованная (unpacked) сборка на Windows host фактически извлекает аудио — это закрывает MEDIA-04 для Windows.
  </what-built>
  <how-to-verify>
    1. **Подготовка scripts.** Если в `package.json` нет script `package`/`build:unpack`, добавить:
       ```
       "build:unpack": "electron-vite build && electron-builder --dir"
       ```
       Если есть — использовать существующий.
    2. **Сборка.** В PowerShell:
       ```
       npm run build:unpack
       ```
       Ожидание: команда завершается без ошибок. В `out/win-unpacked/` появляется exe и `resources/app.asar` + `resources/app.asar.unpacked/node_modules/ffmpeg-static/ffmpeg.exe`.
    3. **Проверка unpacked-бинарников (быстро):**
       ```
       dir out/win-unpacked/resources/app.asar.unpacked/node_modules/ffmpeg-static/
       dir out/win-unpacked/resources/app.asar.unpacked/node_modules/@ffprobe-installer/
       ```
       Должны видеться `ffmpeg.exe` и `ffprobe.exe` (или соответствующая структура per-arch).
    4. **Запуск.** Двойной клик на `out/win-unpacked/scrubber.exe` (или `react-ts.exe` — имя из `electron-builder.yml`).
    5. **Smoke pipeline:**
       a) Окно открывается, вкладка «Транскрипция» доступна.
       b) Перетащить `tests/fixtures/media/short.mp4` в drop-zone — должны появиться meta (имя, размер, длительность ~5 сек).
       c) Нажать «Извлечь аудио». Прогресс должен дойти до 100% (для 5-сек файла — практически мгновенно).
       d) Появляется ExtractDone с путём, заканчивающимся на `.wav`.
       e) В PowerShell: `dir $env:APPDATA\react-ts\extracted\` (или `$env:APPDATA\scrubber\extracted\` — точное имя из productName) — должен лежать `<hash>.wav`.
       f) Опционально: открыть `.wav` в Windows Media Player — играет тон 440Hz.
    6. **Заполнить** `02-VERIFICATION.md` (см. шаблон ниже).
    7. **Linux/macOS:** отметить как SKIPPED («no host available»), зафиксировать gap.
    8. Если на шагах 4-5 что-то падает (typical failures): `ENOENT ffmpeg.exe` → asarUnpack не применился, проверь Plan 01 electron-builder.yml; `EACCES` → ensureExecutable не сработал (но на Windows это не нужно — chmod no-op); UI не открывается → проверить console main process через `out/win-unpacked/scrubber.exe --inspect`.
  </how-to-verify>
  <resume-signal>Type "approved" если Windows packaged smoke прошёл; "failed: <описание>" если есть проблемы; "skipped: <причина>" если по какой-то причине не выполнено</resume-signal>
  <action>
    Создать `.planning/phases/02-media-extraction-pipeline/02-VERIFICATION.md` со следующей структурой (заполнить РЕЗУЛЬТАТАМИ после ручного smoke):

    ```
    # Phase 2 — Verification

    **Phase:** 2 — Media Extraction Pipeline
    **Verified:** <YYYY-MM-DD>
    **Host OS:** Windows 11 Pro 26200
    **Electron version:** 42.x
    **Builder:** electron-builder 26.x

    ## Acceptance Criteria

    | ID | Critierion | Status |
    |----|-----------|--------|
    | MEDIA-01 | File picker через `dialog.showOpenDialog`, filter mp4 | <PASS/FAIL — manual smoke результат> |
    | MEDIA-02 | Drag&drop одного mp4 в окно | <PASS/FAIL> |
    | MEDIA-03 | Извлечение через ffmpeg без зависания UI (cancel работает) | <PASS/FAIL> |
    | MEDIA-04 (Win) | Извлечение работает в packaged-сборке (asarUnpack + path replace) | <PASS/FAIL> |
    | MEDIA-04 (Linux) | То же на Linux | SKIPPED: no host available |
    | MEDIA-04 (macOS) | То же на macOS (+ Gatekeeper note) | SKIPPED: no host available |

    ## Windows Packaged Smoke

    **Date:** <дата>
    **Steps executed:**
    - [x] `npm run build:unpack` — <выход>
    - [x] `out/win-unpacked/...exe` запустился
    - [x] Drop short.mp4 → meta показалось
    - [x] Click `Извлечь аудио` → прогресс до 100%
    - [x] Wav файл в `<userData>/extracted/` — <путь>

    **Issues encountered:** <если есть>

    ## Linux Packaged Smoke

    **Status:** SKIPPED — no Linux host on dev machine (Windows-only)
    **Configuration confidence:** HIGH — asarUnpack + ensureExecutable chmod 0o755 fallback зафиксированы в Plan 01/02; gap для Phase 5 cross-platform CI.

    ## macOS Packaged Smoke

    **Status:** SKIPPED — no macOS host on dev machine
    **Known issues to validate later (Phase 5):** Gatekeeper warning для неподписанного ffmpeg (Pitfall #3); signing/notarization финализируется в Phase 5.

    ## Integration Test Results

    `npm run test:integration` — <PASS/FAIL>
    - ffmpeg binary exists/executable: <PASS>
    - ffprobe returns duration: <PASS>
    - ffmpeg produces 16kHz mono PCM wav: <PASS>
    - parseProgressLine real ffmpeg stdout: <PASS>

    ## Open Gaps for Future Phases

    1. **Linux/macOS packaged smoke** — Phase 5 покрывает в cross-platform CI.
    2. **macOS Gatekeeper / signing** — Phase 5.
    3. **`cacheHit` поле в `MediaExtractResult`** — Plan 03 SUMMARY отметил; добавить в Phase 3 если потребуется.
    4. **`shell.showItemInFolder` IPC** — backlog (Plan 03 заменил «Открыть папку» на «Скопировать путь»).
    ```

    Заполнить таблицу и Windows секцию РЕАЛЬНЫМИ результатами smoke (PASS / FAIL с конкретикой). Если что-то FAIL — НЕ ставить PASS, обновить план или открыть гэп.
  </action>
  <verify>
    <human-check>Окно scrubber.exe открыто; drop работает; wav на диске. PowerShell `dir $env:APPDATA\<productName>\extracted\` показывает файл.</human-check>
    <automated>test -f .planning/phases/02-media-extraction-pipeline/02-VERIFICATION.md</automated>
    <automated>grep -c "Windows Packaged Smoke" .planning/phases/02-media-extraction-pipeline/02-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `02-VERIFICATION.md` создан и содержит секции для всех трёх ОС.
    - Windows секция содержит реальные результаты smoke (не заглушки).
    - Все MEDIA-01..04 в таблице имеют статус (PASS/SKIP с причиной).
    - Если Windows smoke FAIL — задача НЕ approved, требуется исправление кода (новый plan через `/gsd:plan-phase --gaps`).
  </acceptance_criteria>
  <done>VERIFICATION.md заполнен, Windows packaged smoke прошёл, Linux/macOS явно зафиксированы как SKIP. Phase 2 готова к `/gsd:verify-work`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged-exe → user filesystem | Установленное приложение пишет в `<userData>/extracted/`. Никаких других путей. |
| ffmpeg binary unpacked from asar | Бинарник копируется electron-builder'ом; целостность гарантируется build-process (та же бинарка, что в `node_modules/ffmpeg-static/`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | packaged ffmpeg binary | accept | Бинарник из `ffmpeg-static@5.3.0` (Approved). Подпись/нотаризация — Phase 5. На Windows Defender может триггерить — фиксируется в VERIFICATION Pitfall #4. |
| T-02-04-02 | DoS | большой mp4 в smoke | accept | Smoke использует 5-сек fixture; не нагружает систему. |
| T-02-04-03 | Information disclosure | VERIFICATION.md содержит абсолютный путь APPDATA | accept | Это локальный путь host-машины, документ хранится в репо как часть планирования; не secret. |
</threat_model>

<verification>
- `npm run test:integration` зелёный (4+ кейса, реальный ffmpeg).
- `npm run test -- --run` (regular) зелёный (не сломан glob mock).
- `02-VERIFICATION.md` создан, заполнен Windows smoke, Linux/macOS зафиксированы как SKIP.
- Все MEDIA-01..04 имеют статус.
</verification>

<success_criteria>
- Phase 2 acceptance closed для Windows: пользователь на Windows host получает wav из mp4 через упакованную сборку.
- Linux/macOS — гэп задокументирован, переносится в Phase 5 (cross-platform CI).
- Integration test предотвращает регресс в pipeline ffmpeg/ffprobe в dev.
- Фаза готова к `/gsd:verify-work` → `/gsd:plan-phase 03-local-transcription`.
</success_criteria>

<output>
Создать `.planning/phases/02-media-extraction-pipeline/02-04-SUMMARY.md` с:
- Подтверждением, что VERIFICATION.md заполнен и Windows smoke PASS.
- Списком gaps для будущих фаз (Linux/macOS smoke, cacheHit поле, shell.showItemInFolder).
- Указанием, что Phase 2 готова к `/gsd:verify-work`.
</output>
