# Phase 2: Media Extraction Pipeline — Discussion Log

**Session:** 2026-05-30
**Mode:** discuss (default)

Для аудита/ретроспективы. Downstream-агенты читают `02-CONTEXT.md`, а не этот файл.

## Gray Areas Selected

Пользователь выбрал **все 4** предложенные зоны:
- Формат и параметры извлечения аудио
- UX выбора файла и drop-зоны
- Прогресс и отмена извлечения
- Жизненный цикл извлечённого файла

## Q&A

### Зона 1 — Формат извлекаемого аудио

**Q:** Формат извлечённого аудио?
- ✅ WAV 16kHz mono s16le сразу (Whisper-ready) — *Рекомендуемо*
- Универсальный WAV/FLAC
- Настраиваемо пользователем

**Ответ:** WAV 16kHz mono s16le сразу. → D-01, D-02.

### Зона 2 — UX выбора файла

**Q:** Поведение после выбора mp4-файла?
- ✅ Показать метаданные + кнопка «Извлечь аудио» — *Рекомендуемо*
- Авто-старт извлечения
- Без метаданных, просто кнопка Старт

**Ответ:** Метаданные + явная кнопка. → D-03..D-05.

**Q:** Строгость валидации mp4?
- ✅ По расширению .mp4 — *Рекомендуемо*
- Magic bytes (ftyp box)
- Без валидации

**Ответ:** По расширению. → D-06.

**Q:** При drop нескольких файлов?
- ✅ Отклонить с сообщением — *Рекомендуемо*
- Брать первый

**Ответ:** Отклонить. → D-07.

### Зона 3 — Прогресс, Cancel, UI

**Q:** Тип индикатора прогресса извлечения?
- ✅ Проценты через `-progress pipe` — *Рекомендуемо*
- Indeterminate spinner

**Ответ:** %-прогресс через `-progress pipe:1`. → D-08.

**Q:** Cancel и поведение UI во время извлечения?
- ✅ Cancel есть, UI не блокируется — *Рекомендуемо*
- Cancel есть, вкладки отключены
- Без Cancel

**Ответ:** Cancel + UI не блокируется. → D-09, D-10.

### Зона 4 — Хранение и кеш

**Q:** Куда сохранять извлечённый wav?
- ✅ `userData/extracted/<hash>.wav` — *Рекомендуемо*
- `app.getPath('temp')/scrubber/`
- Рядом с mp4

**Ответ:** userData/extracted. → D-11.

**Q:** Политика кеширования и очистки?
- ✅ Кеш по hash, без авточистки в Phase 2 — *Рекомендуемо*
- Без кеша
- Кеш + авточистка при выходе

**Ответ:** Кеш по hash, очистка отложена. → D-12, D-13.

## Deferred Ideas Captured

- Кнопка «Очистить кеш» / TTL-политика в Settings — backlog после Phase 2.
- Форматы кроме mp4 (.mov/.mkv) — v2.
- Встроенный плеер — out of scope.
- Пакетная очередь — out of scope.
- Опции качества аудио (QUAL-*) — v2.

## Claude's Discretion (зафиксировано в CONTEXT)

- JSX/Tailwind layout Transcribe-вкладки.
- Источник ffprobe (`@ffprobe-installer/ffprobe` vs `fluent-ffmpeg.ffprobe` vs парсинг ffmpeg stderr) — ресёрч.
- Форма progress-event subscription helper'а в preload.
- Имена файлов сервисов (`ffmpeg-runner.ts` etc.).
- Логирование job'ов (in-memory).

## Scope Creep — не возникло

Пользователь не предлагал расширений за пределы Phase 2 в этой сессии.

---

*Discussion completed: 2026-05-30*
