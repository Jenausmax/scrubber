---
phase: 03-local-transcription-core-value
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - src/renderer/src/routes/Transcribe.tsx
  - src/renderer/src/components/TranscribeProgress.tsx
  - src/renderer/src/components/TimecodeToggle.tsx
  - src/renderer/src/components/TranscriptResult.tsx
  - src/renderer/src/routes/Transcribe.test.tsx
  - src/main/ipc/transcribe.ts
  - src/main/services/transcriber.ts
  - src/main/utilities/whisper-args.ts
  - tests/integration/transcribe-real.test.ts
  - scripts/smoke-packaged.mjs
autonomous: false
requirements: [TRANS-03, TRANS-04, TRANS-05, TRANS-07]

must_haves:
  truths:
    - "Пользователь видит progress bar с % сверху И живой стриминг распознанных сегментов в окно по мере готовности (TRANS-04, D-11)"
    - "Пользователь может отменить идущую транскрипцию, и ему предлагается сохранить уже распознанный частичный текст (TRANS-05, D-13)"
    - "Тумблер таймкодов пересобирает отображаемый/сохраняемый текст из накопленных сегментов БЕЗ повторного прогона whisper (D-02)"
    - "«Сохранить как» экспортирует transcript.md через системный диалог; дефолтное имя <имя_mp4>.transcript.md формируется в main (по сохранённому audioPath), НЕ в renderer (TRANS-07, D-05)"
    - "Селектор языка (ru/auto/частые) управляет language; ru+VAD по умолчанию (TRANS-03, D-14)"
    - "Реальный whisper-cli транскрибирует короткий русский WAV в integration-тесте и в packaged smoke на Windows (TRANS-01/TRANS-04 интеграционно)"
  artifacts:
    - path: "src/renderer/src/components/TranscribeProgress.tsx"
      provides: "progress bar (role=progressbar) + Cancel + область живого стриминга сегментов"
    - path: "src/renderer/src/components/TimecodeToggle.tsx"
      provides: "тумблер [ЧЧ:ММ:СС], пересборка из сегментов без re-run"
    - path: "tests/integration/transcribe-real.test.ts"
      provides: "реальный whisper-cli + короткий ru WAV → сегменты (gated по наличию бинарника)"
    - path: "scripts/smoke-packaged.mjs"
      provides: "packaged smoke с whisper-cli + DLL транскрибирует ru WAV; self-skip без packaged build"
  key_links:
    - from: "src/renderer/src/routes/Transcribe.tsx"
      to: "transcribe.onSegment"
      via: "живой стриминг сегментов в state"
      pattern: "onSegment"
    - from: "src/renderer/src/components/TimecodeToggle.tsx"
      to: "buildTranscriptMd timecodes"
      via: "пересборка из сегментов в renderer-state"
      pattern: "timecodes"
    - from: "src/main/ipc/transcribe.ts"
      to: "transcriber.getLastAudioPath / defaultName"
      via: "TRANSCRIBE_SAVE_AS handler формирует defaultName из audioPath (main-tier), не из renderer"
      pattern: "defaultName"
---

<objective>
UX-полировка ядра поверх рабочего pipeline (03-02) и управления моделями (03-03): живой стриминг сегментов + %-бар (D-11), отмена с предложением сохранить частичное (D-13), тумблер таймкодов без re-run (D-02), «Сохранить как» (D-05), селектор языка (D-14). Плюс интеграционная и packaged-smoke валидация реальным whisper-cli — финальный gate фазы.

Purpose: Слайс 03-02 дал минимальный happy-path; здесь он становится полноценным UX длительного процесса (видно прогресс + результат сразу, можно отменить не потеряв работу) и проверяется реальным бинарником end-to-end.
Output: TranscribeProgress (бар+стриминг), TimecodeToggle, расширенный TranscriptResult (Сохранить как), селектор языка, partial-save при cancel, TRANSCRIBE_SAVE_AS handler с main-tier defaultName, integration + packaged smoke под whisper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-transcription-core-value/03-RESEARCH.md
@.planning/phases/03-local-transcription-core-value/03-PATTERNS.md
@.planning/phases/03-local-transcription-core-value/03-CONTEXT.md
@.planning/phases/03-local-transcription-core-value/03-02-SUMMARY.md
@.planning/phases/03-local-transcription-core-value/03-03-SUMMARY.md

<interfaces>
<!-- Из 03-02: transcribe.start/cancel/openFile/revealInFolder/onProgress/onSegment в preload; buildTranscriptMd; transcriber singleton хранит audioPath текущего job. -->
shared/ipc.ts: Channels.TRANSCRIBE_SAVE_AS, TranscribeApi.saveAs(md, defaultName?)→Result<{path}|null>, onSegment(cb:{jobId,startMs,text})
transcript-builder.ts: buildTranscriptMd(segments, meta, {timecodes}) — пересборка без re-run (D-02)
whisper-args.ts: buildTranscribeArgs — добавить опциональные VAD-тюнинг флаги при необходимости
transcriber.ts (03-02): singleton; хранит audioPath последнего/текущего job (для main-tier defaultName)

<!-- Эталоны -->
ExtractProgress.tsx:21-51 — role=progressbar + aria-valuenow + clamp + Cancel → эталон TranscribeProgress
Transcribe.tsx: useEffect+stateRef:50-73 (две подписки onProgress+onSegment), handleCancel:135-140
media.ts: dialog.showOpenDialog:62-82 → эталон для dialog.showSaveDialog (saveAs)
scripts/smoke-packaged.mjs — существующий packaged smoke (ffmpeg) → расширить под whisper-cli+DLL
tests/integration (extract-real gated по бинарнику) — эталон gating
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Живой стриминг + %-бар + cancel-partial + тумблер + Сохранить как (main-tier defaultName) + язык</name>
  <read_first>
    - src/renderer/src/components/ExtractProgress.tsx (21-51 — role=progressbar+aria+Cancel)
    - src/renderer/src/routes/Transcribe.tsx (FSM из 03-02/03-03; useEffect+stateRef:50-73; handleCancel:135-140)
    - src/renderer/src/components/TranscriptResult.tsx (из 03-02 — добавить «Сохранить как» + тумблер)
    - src/main/services/transcript-builder.ts (buildTranscriptMd timecodes — из 03-02)
    - src/main/services/transcriber.ts (из 03-02 — singleton, audioPath текущего job; добавить геттер для main-tier defaultName)
    - src/main/ipc/transcribe.ts (из 03-02 — добавить TRANSCRIBE_SAVE_AS handler; эталон saveAs — media.ts dialog.showOpenDialog:62-82)
    - 03-RESEARCH.md §"Pattern 2 Cancel+partial" (270-272), §"VAD Parameters" (175-195), §"Code Examples" (357-401)
    - 03-PATTERNS.md §"Transcribe.tsx" (334-339), §"Transcribe* компоненты" (340-345), §"saveAs/openFile" (260)
  </read_first>
  <behavior>
    - onSegment аппендит сегменты в renderer-state по мере прихода → SegmentStream область растёт во время transcribing (TRANS-04, D-11)
    - %-бар (role=progressbar, aria-valuenow, clamp 0-100) обновляется от onProgress (TRANS-04, D-11)
    - Cancel во время transcribing → transcribe.cancel(jobId); reason cancelled → UI предлагает «Сохранить частичное?» из накопленных сегментов (НЕ сброс в idle) → transcribe.saveAs(buildTranscriptMd(partialSegments,...)) (TRANS-05, D-13)
    - TimecodeToggle (по умолчанию OFF, D-02): переключение пересобирает отображаемый текст из тех же сегментов через buildTranscriptMd({timecodes}) БЕЗ повторного start
    - «Сохранить как»: renderer вызывает transcribe.saveAs(md) (БЕЗ передачи имени из renderer); defaultName='<basename audioPath>.transcript.md' формирует MAIN по сохранённому в transcriber audioPath (untrusted renderer не формирует имя файла) → showSaveDialog (TRANS-07, D-05)
    - селектор языка ru(default)/auto/частые → передаётся в transcribe.start({language}); ru+VAD дефолт (TRANS-03, D-14)
  </behavior>
  <action>
    (1) src/renderer/src/components/TranscribeProgress.tsx (← ExtractProgress.tsx): stateless role=progressbar + aria-valuenow + clamp + Cancel-кнопка; ниже бара — SegmentStream область, рендерящая накопленные сегменты (props: percent, segments, onCancel). Русские строки.
    (2) src/renderer/src/components/TimecodeToggle.tsx: тумблер (label «Таймкоды», default OFF, D-02); onChange → renderer пересобирает текст из segments через локальную пересборку (формат [ЧЧ:ММ:СС] text) либо общий хелпер; без IPC, без re-run.
    (3) src/renderer/src/components/TranscriptResult.tsx (расширить из 03-02): добавить кнопку «Сохранить как» → transcribe.saveAs(md) БЕЗ имени (имя формирует main). Интегрировать TimecodeToggle (переключает отображаемый текст). Сохранить кнопки Открыть/Показать в папке.
    (4) src/renderer/src/routes/Transcribe.tsx: добавить вторую подписку transcribe.onSegment в тот же useEffect (stateRef для актуального state); state transcribing накапливает segments[]; на cancelled — состояние transcript-cancelled-partial с предложением сохранить (НЕ idle). Добавить селектор языка (ru/auto/частые) над кнопкой «Транскрибировать»; language персистится через settings-store (03-03). handleCancel → transcribe.cancel(jobId).
    (5) src/main/services/transcriber.ts: добавить публичный getter getCurrentAudioPath() (или хранить lastAudioPath) — возвращает audioPath активного/последнего job. Это источник истины для defaultName на стороне main (renderer не передаёт имя).
    (6) src/main/ipc/transcribe.ts: реализовать TRANSCRIBE_SAVE_AS handler — сигнатура saveAs(md): вычислить defaultName в MAIN: basename(transcriber.getCurrentAudioPath() ?? 'transcript') + '.transcript.md' (untrusted renderer-имя не используется, T-3-06/warning-5); dialog.showSaveDialog({defaultPath: defaultName, filters:[{name:'Markdown', extensions:['md']}]}) → если не отменён, fs.writeFile(path, md) → Result<{path}> | Result<null> при отмене (путь только из dialog-результата). LOG_PREFIX '[ipc/transcribe]'. saveAs принимает ТОЛЬКО md-контент; defaultName сигнатуры TranscribeApi.saveAs становится опциональным/игнорируемым на стороне main (main всегда формирует имя сам).
    (7) (опц.) src/main/utilities/whisper-args.ts: добавить поддержку VAD-тюнинг флагов (--vad-threshold, --vad-min-speech-duration-ms) как опциональных полей TranscribeOpts на случай gap'а качества из 03-02-чекпоинта; дефолты не менять без UAT-сигнала.
  </action>
  <verify>
    <automated>npm run test:unit -- src/renderer/src/routes/Transcribe.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - RTL: mock onSegment-события аппендят сегменты в стриминг-область во время transcribing; onProgress двигает бар (TRANS-04)
    - RTL: Cancel → transcribe.cancel; cancelled → показано предложение «Сохранить частичное», клик → transcribe.saveAs с накопленным md (TRANS-05/D-13)
    - RTL: тумблер таймкодов переключает отображаемый текст без повторного transcribe.start (D-02)
    - RTL: «Сохранить как» → transcribe.saveAs вызывается с md-контентом и БЕЗ имени из renderer; grep transcribe.ts подтверждает формирование defaultName из transcriber.getCurrentAudioPath()/basename в main (D-05, warning-5)
    - селектор языка передаёт language в transcribe.start; typecheck зелёный
  </acceptance_criteria>
  <done>Полноценный UX длительной транскрипции: live-стриминг+бар (TRANS-04), cancel-partial (TRANS-05/D-13), тумблер (D-02), Сохранить как с main-tier defaultName (D-05), язык (D-14).</done>
</task>

<task type="auto">
  <name>Task 2: Integration-тест (реальный whisper-cli) + packaged smoke под whisper</name>
  <read_first>
    - tests/integration/transcribe-real.test.ts (RED-стаб из 03-01)
    - tests/integration (существующий extract-real — эталон gating по наличию бинарника)
    - scripts/smoke-packaged.mjs (существующий ffmpeg packaged smoke — расширить)
    - src/main/services/whisper-paths.ts, src/main/utilities/whisper-args.ts
    - 03-RESEARCH.md §"Validation Architecture" (463-503), §"Cross-Platform Delivery" (143-158), §"Common Pitfalls" Pitfall 1 (321-325)
  </read_first>
  <behavior>
    - integration: если whisper-cli + tiny/small модель доступны → транскрибировать короткий русский WAV-фикстуру → получить ≥1 сегмент с непустым текстом; иначе тест skip (gated, как extract-real) — НЕ падать в CI без бинарника (TRANS-01)
    - packaged smoke (scripts/smoke-packaged.mjs): запускается командой `node scripts/smoke-packaged.mjs`. Если packaged build (app.asar.unpacked) отсутствует → скрипт печатает SKIP и exit 0 (НЕ ложный зелёный: явный SKIP-лог, не «passed»). Если build есть → проверяет whisper-cli.exe + DLL рядом в app.asar.unpacked (Pitfall 1) и транскрибирует короткий ru WAV → exit 0 при успехе, exit≠0 при провале.
  </behavior>
  <action>
    (1) tests/integration/transcribe-real.test.ts (GREEN, gated): подготовить короткую русскую WAV-фикстуру (16kHz mono s16le, несколько секунд). Резолвить whisper-cli через whisper-paths; если бинарник/модель отсутствуют → test.skip (зеркало extract-real gating). Запустить реальный whisper-cli с buildTranscribeArgs (tiny/small для скорости в тесте) → распарсить -oj JSON → assert ≥1 сегмент непустого текста. Таймаут увеличить (реальная транскрипция).
    (2) scripts/smoke-packaged.mjs: расширить под whisper — самодостаточный mjs-скрипт с явным gating: detect app.asar.unpacked (через build:unpack-вывод); если нет → console.log('[smoke] SKIP: packaged build not found') + process.exit(0); если есть → проверить, что resources/whisper/win32-x64/whisper-cli.exe + DLL присутствуют в app.asar.unpacked; запустить whisper-cli.exe из распакованного пути на короткой ru WAV-фикстуре (с положенной tiny/small моделью) → подтвердить генерацию транскрипта/JSON и exit 0 (валидирует Pitfall 1); при отсутствии DLL или ненулевом коде → console.error + process.exit(1). Логировать результат явно (PASS/FAIL/SKIP).
  </action>
  <verify>
    <automated>npm run test:unit -- tests/integration/transcribe-real.test.ts && node scripts/smoke-packaged.mjs</automated>
  </verify>
  <acceptance_criteria>
    - transcribe-real.test.ts зелёный или явно skip при отсутствии бинарника (НЕ fail) — grep подтверждает gating
    - `node scripts/smoke-packaged.mjs` завершается exit 0: либо явный SKIP-лог при отсутствии packaged build, либо PASS при наличии build+DLL+бинарника (НЕ ложный зелёный без проверки — SKIP логируется отдельно от PASS)
    - scripts/smoke-packaged.mjs содержит ветку проверки whisper-cli + DLL в app.asar.unpacked и явный exit(1) при провале
    - при наличии бинарника+модели integration возвращает непустой сегмент русского текста
  </acceptance_criteria>
  <done>Реальный whisper-cli валидируется интеграционно и через `node scripts/smoke-packaged.mjs` (gated, без ложного зелёного; Pitfall 1 закрыт). Финальная packaged-валидация на Windows подтверждается в checkpoint ниже (manual UAT).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    Полный UX-слой и packaged-валидация. Требуется ручной end-to-end чек на реальном русском mp4 в packaged-сборке Windows: live-стриминг, отмена с сохранением частичного, тумблер таймкодов, экспорт, качество русского без галлюцинаций (ядро ценности, TRANS-03 manual UAT). Packaged smoke (scripts/smoke-packaged.mjs) автоматизирован, но финальный запуск на реальной Windows-сборке подтверждается здесь вручную.
  </what-built>
  <how-to-verify>
    1. `npm run build` + установить/запустить packaged-сборку (или build:unpack). Затем `node scripts/smoke-packaged.mjs` → подтвердить PASS (не SKIP) — whisper-cli из app.asar.unpacked отработал (Pitfall 1).
    2. В Settings → Модели скачать medium (прогресс + проверить, что по завершении статус «скачана»; при желании отменить и убедиться, что .tmp удаляется).
    3. На Transcribe: реальный русский mp4 → Извлечь → Транскрибировать. Наблюдать: %-бар двигается И сегменты появляются по мере распознавания (D-11/TRANS-04). Если сегменты приходят батчем в конце — зафиксировать (A5, деградация UX, не блокер).
    4. Отменить на середине → подтвердить предложение «Сохранить частичное» → сохранить → открыть .md, проверить что частичный текст на месте (D-13/TRANS-05).
    5. Запустить заново до конца. Переключить тумблер таймкодов → текст пересобирается с [ЧЧ:ММ:СС] мгновенно, без повторной транскрипции (D-02).
    6. «Сохранить как» → дефолтное имя <имя_mp4>.transcript.md в диалоге (D-05, имя сформировано main); открыть/показать в папке (D-06).
    7. КРИТИЧНО: качество русского осмысленное, нет галлюцинаций на тишине. Если есть — применить VAD-тюнинг (--vad-threshold 0.6 и т.п.) в whisper-args и перепроверить.
  </how-to-verify>
  <resume-signal>Напишите "approved" если все 5 success criteria фазы выполнены, packaged smoke PASS и русский транскрипт качественный, либо опишите gap'ы (качество/галлюцинации/стриминг/packaged)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→main (saveAs) | md-контент из renderer пишется в выбранный пользователем путь; имя файла НЕ берётся из renderer |
| main→fs (showSaveDialog путь) | Путь записи — только из системного диалога (доверенный); defaultName формирует main из audioPath |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06 | Tampering/Path-traversal | transcribe.saveAs путь записи + имя | mitigate | Путь ТОЛЬКО из dialog.showSaveDialog (доверенный); defaultName формируется в main из transcriber.audioPath (basename), НЕ из renderer-строки; md-контент — текст, не исполняемый |
| T-3-10 | Tampering | whisper-cli из app.asar.unpacked в packaged | mitigate | packaged smoke проверяет .exe+DLL рядом (Pitfall 1); бинарник из официального релиза (T-3-02) |
| T-3-SC | Tampering | npm install | accept | Phase 3 не добавляет npm-пакетов (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- npm run test:unit зелёный для Transcribe.test + transcribe-real (или skip)
- npm run typecheck зелёный
- npm test (full suite) зелёный перед /gsd:verify-work
- `node scripts/smoke-packaged.mjs` exit 0 (SKIP без build / PASS с build на Windows)
- Чекпоинт: 5 success criteria фазы выполнены, packaged smoke PASS, русский качественный без галлюцинаций (manual UAT)
</verification>

<success_criteria>
- Live-стриминг сегментов + %-бар (TRANS-04, D-11)
- Cancel с сохранением частичного (TRANS-05, D-13)
- Тумблер таймкодов без re-run (D-02)
- Сохранить как <имя_mp4>.transcript.md, имя формирует main (TRANS-07, D-05)
- Селектор языка, ru+VAD дефолт (TRANS-03, D-14)
- Реальный whisper-cli валидирован integration + packaged smoke (TRANS-01)
</success_criteria>

<output>
Create `.planning/phases/03-local-transcription-core-value/03-04-SUMMARY.md` when done
</output>
</content>
