---
phase: 03-local-transcription-core-value
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/main/services/model-manager.ts
  - src/main/services/model-manager.test.ts
  - src/main/ipc/models.ts
  - src/main/ipc/index.ts
  - src/main/services/settings-store.ts
  - src/preload/index.ts
  - src/renderer/src/routes/Settings.tsx
  - src/renderer/src/routes/Settings.test.tsx
  - src/renderer/src/routes/Transcribe.tsx
autonomous: false
requirements: [TRANS-02]

must_haves:
  truths:
    - "Пользователь видит в Settings список моделей small/medium/large-v3 (turbo исключён) с размерами и статусом «скачана/нет» и может скачать модель по запросу (TRANS-02, D-07, D-09)"
    - "Скачивание идёт с progress bar + кнопкой Отмена + SHA256-проверкой целостности; sha-mismatch → файл удаляется и помечается failed (D-10)"
    - "Модели НЕ бандлятся — качаются в userData/models (TRANS-02)"
    - "На вкладке Transcribe при отсутствии скачанной модели транскрипция заблокирована с понятным сообщением и отсылкой в Settings (D-09, model_missing)"
    - "Дефолтно предвыбрана модель medium; выбор модели/языка персистится; timecodesEnabled (D-02 default ВЫКЛ) персистится в settings-store как состояние UI-тумблера (применяется renderer-side в 03-04, НЕ влияет на auto-save, который по D-01 всегда пишет сплошной текст) (D-08)"
  artifacts:
    - path: "src/main/services/model-manager.ts"
      provides: "download(name) stream→.tmp→SHA256→rename; cancel(AbortController); delete; list; manifest URL+SHA+size"
      exports: ["modelManager", "MODEL_MANIFEST"]
    - path: "src/main/ipc/models.ts"
      provides: "MODELS_LIST/DOWNLOAD/CANCEL/DELETE handlers с whitelist model-name (анти-SSRF)"
      exports: ["registerModelsHandlers"]
    - path: "src/renderer/src/routes/Settings.tsx"
      provides: "раздел «Модели»: список+размеры+статус+Скачать(прогресс+отмена)/Удалить + персист выбора модели/языка/таймкодов"
    - path: "src/renderer/src/routes/Settings.test.tsx"
      provides: "RTL-тесты: список моделей рендерится, Скачать/Удалить вызывают IPC, model_missing-сообщение"
  key_links:
    - from: "src/main/ipc/models.ts"
      to: "MODEL_WHITELIST"
      via: "валидация model-name перед построением URL"
      pattern: "MODEL_WHITELIST"
    - from: "src/main/services/model-manager.ts"
      to: "createHash('sha256')"
      via: "проверка целостности по манифесту"
      pattern: "sha256"
    - from: "src/renderer/src/routes/Transcribe.tsx"
      to: "model_missing block"
      via: "проверка наличия модели / reason model_missing → отсылка в Settings"
      pattern: "model_missing"
    - from: "src/renderer/src/routes/Settings.tsx"
      to: "settings-store.timecodesEnabled"
      via: "тумблер таймкодов персистит состояние в settings-store (D-02 default ВЫКЛ). ВАЖНО: это состояние UI-тумблера, НЕ поле IPC-контракта. Auto-save (D-04) по D-01 ВСЕГДА пишет сплошной текст (timecodes:false); тумблер применяется renderer-side в 03-04 как пересборка из сохранённых сегментов БЕЗ re-run whisper. НЕ добавлять поле timecodes в TranscribeApi.start / shared/ipc.ts."
      pattern: "timecodesEnabled"
---

<objective>
Вертикальный слайс управления моделями: пользователь скачивает/удаляет модели Whisper в Settings (прогресс, отмена, SHA-проверка), а вкладка Transcribe блокирует транскрипцию при отсутствии модели и отсылает в Settings. Это снимает ручную подкладку модели из 03-02 — теперь модель появляется через UI. Закрывает TRANS-02 и «модели не бандлятся».

Purpose: Core-value pipeline из 03-02 требовал вручную положенной модели; этот слайс даёт пользователю самостоятельно получить модель по запросу — обязательное требование v1 (модели ~0.5–3 ГБ не бандлятся в установщик).
Output: model-manager.ts (download+SHA+cancel+delete+list), ipc/models.ts (whitelist), Settings «Модели», расширенный settings-store, model_missing-блок на Transcribe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-transcription-core-value/03-RESEARCH.md
@.planning/phases/03-local-transcription-core-value/03-PATTERNS.md
@.planning/phases/03-local-transcription-core-value/03-CONTEXT.md
@.planning/phases/03-local-transcription-core-value/03-02-SUMMARY.md

<interfaces>
<!-- Из 03-01/03-02: контракт models.* в shared/ipc.ts; whisper-paths.resolveModel. -->
shared/ipc.ts: Channels.MODELS_LIST/DOWNLOAD/CANCEL/DELETE/PROGRESS, ModelReason, ModelsApi, ModelProgressEvent
whisper-paths.ts: resolveModel(name) → userData/models/ggml-<name>.bin, resolveVadModel()

<!-- Манифест (03-RESEARCH.md:160-173) — base URL https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ ; VAD https://huggingface.co/ggml-org/whisper-vad/resolve/main/ -->
small    ggml-small.bin    SHA256 1be3a9b2063867b937e64e2ec7483364a79917e157fa98c5d94b5c1fffea987b  ~511,705,088 B
medium   ggml-medium.bin   SHA256 6c14d5adee5f86394037b4e4e8b59f1673b6cee10e3cf0b11bbdbee79c156208  ~1.53 GB
large-v3 ggml-large-v3.bin SHA256 64d182b440b98d5203c4f9bd541544d84c605196c4f7b845dfa11fb23594d1e2  ~3.1 GB
silero   ggml-silero-v5.1.2.bin SHA256 29940d98d42b91fbd05ce489f3ecf7c72f0a42f027e4875919a28fb4c04ea2cf ~885 kB

<!-- Эталоны -->
media-extractor.ts: .tmp→rename:178-179,322-330; job-map+cancel:71,334-340; createHash:30,58-60; emitProgress→webContents.send:224-232
media.ts: handle-wrapper:62-135; UUID_REGEX:30
settings-store.ts: SettingsSchema:15-17 (generic-класс, менять только интерфейс)
Settings.tsx: useEffect-загрузка + Result-маппинг + Status-union:14-38
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: model-manager (download+SHA256+cancel+delete+list) + ipc/models (whitelist)</name>
  <read_first>
    - src/main/services/media-extractor.ts (.tmp→rename:178-179,322-330; job-map+cancel:71,334-340; createHash:30,58-60; emitProgress:224-232)
    - src/main/services/model-manager.test.ts (RED-стаб из 03-01 — здесь GREEN)
    - src/main/ipc/media.ts (handle-wrapper + UUID_REGEX)
    - src/main/ipc/index.ts (registry)
    - src/main/services/whisper-paths.ts (resolveModel/resolveVadModel)
    - 03-RESEARCH.md §"Model Manifest" (160-173), §"Pattern 4 Model download" (278-280), §"Security Domain" (505-526)
    - 03-PATTERNS.md §"model-manager.ts" (277-285), §"ipc/models.ts" (264-273)
  </read_first>
  <behavior>
    - modelManager.list() → массив {name, sizeBytes, downloaded} для small/medium/large-v3 (downloaded = existsSync(resolveModel))
    - download(name): stream fetch(manifest URL) → userData/models/ggml-<name>.bin.tmp; прогресс по Content-Length → emit MODELS_PROGRESS {name, percent}; по завершении createHash('sha256') == манифест → rename .tmp→final, иначе unlink + reason 'sha_mismatch' (D-10)
    - download cancel: AbortController.abort() + unlink .tmp → reason 'cancelled'; resume НЕ делается
    - сетевая ошибка → 'download_failed'; ENOSPC → 'disk_full'
    - delete(name) → unlink ggml-<name>.bin; отсутствует → ok (идемпотентно)
    - ipc/models: model-name ВНЕ whitelist {small,medium,large-v3} → reason 'invalid_argument' (URL строится ТОЛЬКО из манифеста, анти-SSRF); cancel jobId UUID_REGEX
    - silero VAD при первом download основной модели: СНАЧАЛА обеспечить наличие silero (если ещё нет — скачать+SHA), ЗАТЕМ качать основную модель; частичный успех (silero ok, основная failed / наоборот) → reason относится к основной модели, silero-результат логируется отдельно и не блокирует повтор
  </behavior>
  <action>
    (1) src/main/services/model-manager.ts: экспортировать MODEL_MANIFEST (Record name→{file,url,sizeBytes,sha256}) из interfaces-блока + silero VAD-запись. Singleton modelManager: list(), download(name) — fetch(url, {signal}) → стрим в resolveModel(name)+'.tmp', считать байты vs Content-Length → emitProgress(webContents.send MODELS_PROGRESS), на end createHash('sha256') over файла → сверка с манифестом → fs.rename .tmp→final | unlink + reason sha_mismatch; in-memory job-map {jobId→AbortController}; cancel(jobId) → abort + unlink .tmp; delete(name) → unlink (идемпотентно). reason-маппинг download_failed/sha_mismatch/cancelled/disk_full/internal. LOG_PREFIX '[services/model-manager]'. Снять skip в model-manager.test.ts → GREEN (мок fetch + sha mismatch кейс).
    (2) src/main/ipc/models.ts: registerModelsHandlers() с handle-обёртками. MODEL_WHITELIST = new Set(['small','medium','large-v3']); MODELS_DOWNLOAD/DELETE валидируют name ∈ whitelist → иначе invalid_argument; MODELS_CANCEL — UUID_REGEX; MODELS_LIST → modelManager.list(). MODELS_PROGRESS — event-канал (НЕ handle).
    (3) src/main/ipc/index.ts — добавить registerModelsHandlers() в registerIpcHandlers.
    silero VAD: модель крошечная (~885 kB). Порядок при первом download любой основной модели — внутри download(name) сначала ensureSilero() (existsSync(resolveVadModel()) ? noop : скачать+SHA-проверить как обычную запись манифеста), затем стрим основной модели. Если silero падает — логировать, но дать основной модели попытку (VAD опционален: transcriber.ts из 03-02 передаёт vadModelPath=null при отсутствии silero). НЕ делать silero блокирующей предпосылкой основной модели.
  </action>
  <verify>
    <automated>npm run test:unit -- src/main/services/model-manager.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - model-manager.test.ts зелёный: успешное скачивание (мок fetch) → rename; sha-mismatch → unlink + reason sha_mismatch; cancel → abort + unlink
    - grep model-manager.ts подтверждает createHash('sha256') и .tmp→rename
    - grep models.ts подтверждает MODEL_WHITELIST и отсутствие конкатенации renderer-строки в URL
    - ipc/index.ts вызывает registerModelsHandlers(); typecheck зелёный
  </acceptance_criteria>
  <done>Backend моделей: скачивание с SHA-проверкой, отмена, удаление, список; whitelist защищает от SSRF; модели в userData (не бандлятся); silero обеспечивается до основной модели без блокировки.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings «Модели» + settings-store + preload models + model_missing-блок на Transcribe</name>
  <read_first>
    - src/renderer/src/routes/Settings.tsx (useEffect-загрузка + Result-маппинг + Status-union:14-38)
    - src/renderer/src/routes/Settings.test.tsx (если есть — эталон RTL для Settings; иначе создаётся в этой задаче по образцу Transcribe.test.tsx)
    - src/main/services/settings-store.ts (SettingsSchema:15-17 — generic-класс)
    - src/preload/index.ts (namespace media :31-43 — эталон для models)
    - src/renderer/src/routes/Transcribe.tsx (extract/transcribe FSM из 03-02)
    - 03-PATTERNS.md §"Settings.tsx" (346), §"settings-store.ts" (348), §"preload" (319-328)
  </read_first>
  <behavior>
    - Settings показывает 3 модели с размерами и статусом; «Скачать» → прогресс + «Отмена»; «Удалить» доступна для скачанной (D-09)
    - выбор модели (дефолт medium, D-08) и языка (ru) персистятся через settings-store; timecodesEnabled (default false, D-02) тоже в схеме и читаемо/записываемо
    - Transcribe: если выбранной модели нет на диске (или start вернул model_missing) → кнопка транскрипции заблокирована + сообщение с отсылкой в Settings (D-09)
    - Transcribe прокидывает timecodesEnabled из settings-store в transcribe.start({..., timecodes}) — чтобы auto-save в transcriber.ts (03-02) уважал настройку (D-02 default ВЫКЛ); тумблер в окне (03-04) делает только UI-пересборку из тех же сегментов
    - все строки русские; download-прогресс через scrubber.models.onProgress
  </behavior>
  <action>
    (1) src/main/services/settings-store.ts: расширить SettingsSchema — добавить selectedModel?: string (дефолт 'medium'), selectedLanguage?: string (дефолт 'ru'), timecodesEnabled?: boolean (дефолт false, D-02). Менять только интерфейс схемы (класс generic). При необходимости — settings IPC для чтения/записи этих ключей (зеркало существующих settings-handlers) либо переиспользовать имеющиеся.
    (2) src/preload/index.ts: добавить namespace models { list, download, cancel, delete, onProgress(ipcRenderer.on MODELS_PROGRESS + unsubscribe) } — зеркало media; каналы только Channels.*.
    (3) src/renderer/src/routes/Settings.tsx: добавить раздел «Модели» — useEffect → scrubber.models.list(); рендер списка small/medium/large-v3 c размерами + статус «скачана/нет»; кнопка «Скачать» (scrubber.models.download → подписка onProgress → progress bar + «Отмена» scrubber.models.cancel); «Удалить» (scrubber.models.delete). Status-union паттерн :14-38. Селект выбранной модели (дефолт medium) + языка (ru/auto/...) + чекбокс timecodesEnabled (default OFF) с персистом в settings-store.
    (4) src/renderer/src/routes/Settings.test.tsx: создать (по образцу Transcribe.test.tsx) — мок scrubber.models; тесты: список моделей рендерится с размерами/статусом; клик «Скачать» вызывает scrubber.models.download и показывает прогресс; «Удалить» вызывает scrubber.models.delete; персист selectedModel/timecodesEnabled пишет в settings-store. Этот файл — цель <verify> данной задачи.
    (5) src/renderer/src/routes/Transcribe.tsx: перед стартом транскрипции проверить наличие выбранной модели (через models.list / или ловить reason model_missing из start) → если нет: заблокировать кнопку «Транскрибировать» и показать сообщение «Модель не скачана — перейдите в Настройки → Модели» (D-09). Использовать REASON_COPY[model_missing] из InlineError (03-02). При старте читать settings-store.timecodesEnabled и передавать в transcribe.start({..., timecodes}) (D-02 default ВЫКЛ) — фиксирует, что auto-save (transcriber.ts) уважает настройку таймкодов.
  </action>
  <verify>
    <automated>npm run test:unit -- src/renderer/src/routes/Settings.test.tsx src/renderer/src/routes/Transcribe.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - RTL Settings (Settings.test.tsx): список моделей рендерится; клик «Скачать» вызывает scrubber.models.download и показывает прогресс; «Удалить» вызывает scrubber.models.delete; этот тест-файл проверяет model_missing-связку и персист настроек
    - RTL Transcribe: при model_missing кнопка транскрипции заблокирована и показано сообщение с отсылкой в Settings; transcribe.start вызывается с полем timecodes из settings-store
    - settings-store SettingsSchema содержит selectedModel/selectedLanguage/timecodesEnabled; typecheck зелёный
    - preload экспонирует models namespace
  </acceptance_criteria>
  <done>Пользователь самостоятельно скачивает/удаляет модели в Settings; Transcribe корректно блокирует без модели и прокидывает timecodesEnabled в auto-save pipeline. TRANS-02 закрыт.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→main (models.download) | model-name из renderer — недоверенный; valid только whitelist |
| main→HuggingFace (HTTPS fetch) | Скачивание модели; целостность не гарантирована каналом |
| network→fs (.tmp файл) | Частично скачанный файл не должен использоваться как готовый |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-07 | Tampering/Info-disclosure (SSRF) | models.download model-name → URL | mitigate | MODEL_WHITELIST в main; URL ТОЛЬКО из pinned MODEL_MANIFEST, никогда из renderer-строки |
| T-3-08 | Tampering | скачанная модель (MITM/перезалив) | mitigate | createHash('sha256') сверка с pinned-манифестом ПЕРЕД использованием (D-10); mismatch→unlink |
| T-3-09 | DoS/Integrity | обрыв скачивания ~3 ГБ | mitigate | .tmp→rename атомарность; неполный файл не проходит SHA → unlink + перекачка (resume НЕ делаем, D-10) |
| T-3-SC | Tampering | npm install | accept | Phase 3 не добавляет npm-пакетов (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- npm run test:unit зелёный для model-manager / Settings.test.tsx / Transcribe.test.tsx
- npm run typecheck зелёный
- grep подтверждает whitelist + sha256 + .tmp→rename
</verification>

<success_criteria>
- Скачивание/удаление моделей в Settings с прогрессом, отменой, SHA-проверкой (TRANS-02, D-09, D-10)
- Модели в userData (не бандлятся)
- Transcribe блокирует без модели и отсылает в Settings (D-09)
- Дефолт medium, выбор персистится; timecodesEnabled доезжает до auto-save (D-08, D-02)
</success_criteria>

<output>
Create `.planning/phases/03-local-transcription-core-value/03-03-SUMMARY.md` when done
</output>
</content>
</invoke>
