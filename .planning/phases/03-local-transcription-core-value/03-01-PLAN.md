---
phase: 03-local-transcription-core-value
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/ipc.ts
  - src/main/utilities/whisper-args.ts
  - src/main/utilities/whisper-args.test.ts
  - src/main/utilities/whisper-runner-parse.test.ts
  - src/main/services/whisper-paths.ts
  - src/main/services/transcriber.test.ts
  - src/main/services/model-manager.test.ts
  - src/main/services/transcript-builder.test.ts
  - tests/integration/transcribe-real.test.ts
  - resources/whisper/win32-x64/
autonomous: false
requirements: [TRANS-01, TRANS-02, TRANS-03, TRANS-04, TRANS-05, TRANS-06, TRANS-07]

must_haves:
  truths:
    - "Контракт transcribe.* / models.* существует в shared/ipc.ts и типобезопасен (TranscribeApi, ModelsApi, TranscribeReason, ModelReason, новые Channels)"
    - "Все Wave 0 тест-стабы существуют и RED (падают, т.к. реализации ещё нет) — Nyquist-семплинг готов для последующих волн"
    - "buildTranscribeArgs собирает -m/-l ru/-pp/-oj/-t/-f и --vad/-vm при наличии VAD-модели (TRANS-03)"
    - "whisper-cli.exe + все DLL физически лежат в resources/whisper/win32-x64/ (in-tree, в git)"
  artifacts:
    - path: "src/shared/ipc.ts"
      provides: "transcribe.*/models.* Channels + TranscribeApi/ModelsApi/TranscribeReason/ModelReason + ScrubberApi extension"
      contains: "TranscribeApi"
    - path: "src/main/utilities/whisper-args.ts"
      provides: "buildTranscribeArgs pure function"
      exports: ["buildTranscribeArgs", "TranscribeOpts"]
    - path: "src/main/utilities/whisper-args.test.ts"
      provides: "TRANS-03 flag assembly tests"
    - path: "src/main/utilities/whisper-runner-parse.test.ts"
      provides: "TRANS-04 segment(stdout)/progress(stderr) regex tests"
    - path: "src/main/services/whisper-paths.ts"
      provides: "resolveWhisperCli/resolveModel/resolveVadModel/assertBinaryExists/assertModelExists/ensureExecutable"
      exports: ["resolveWhisperCli", "resolveModel", "assertBinaryExists"]
    - path: "resources/whisper/win32-x64/whisper-cli.exe"
      provides: "whisper.cpp v1.8.6 Windows x64 binary"
  key_links:
    - from: "src/shared/ipc.ts"
      to: "ScrubberApi"
      via: "transcribe: TranscribeApi; models: ModelsApi"
      pattern: "transcribe:\\s*TranscribeApi"
    - from: "src/main/utilities/whisper-args.ts"
      to: "whisper-cli flags"
      via: "buildTranscribeArgs returns -l ru --vad"
      pattern: "'--vad'"
---

<objective>
Заложить общие контракты и Nyquist-валидационную инфраструктуру (Wave 0) для всей фазы 3, плюс физически подготовить whisper-cli-бинарник. Это scaffolding под последующие вертикальные слайсы (core-value транскрипция → управление моделями → live-UX), а НЕ горизонтальный слой: тесты пишутся RED заранее, чтобы каждый последующий слайс семплировался автоматически.

Purpose: Без расширенного IPC-контракта и тест-стабов нельзя реализовать ни один вертикальный слайс детерминированно — исполнители получают контракты в плане, а не ищут их в коде.
Output: Расширенный shared/ipc.ts, чистая функция buildTranscribeArgs + её тесты, тесты парсинга runner, whisper-paths.ts, RED-стабы для transcriber/model-manager/transcript-builder/integration, подготовленные resources/whisper/win32-x64/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-transcription-core-value/03-RESEARCH.md
@.planning/phases/03-local-transcription-core-value/03-PATTERNS.md
@.planning/phases/03-local-transcription-core-value/03-CONTEXT.md

<interfaces>
<!-- Существующий контракт (src/shared/ipc.ts) — расширяем его, НЕ переписываем. -->
Уже в shared/ipc.ts: Result<T>, Channels (SETTINGS_*, MEDIA_*), MediaReason, MediaApi, ScrubberApi { settings, media, getPathForFile }, MediaProgressEvent.

Целевые добавления (точные сигнатуры — 03-RESEARCH.md:420-451):
```
Channels += TRANSCRIBE_START 'transcribe:start' / TRANSCRIBE_CANCEL 'transcribe:cancel'
          / TRANSCRIBE_PROGRESS 'transcribe:progress' / TRANSCRIBE_SEGMENT 'transcribe:segment'
          / TRANSCRIBE_SAVE_AS 'transcribe:saveAs' / TRANSCRIBE_OPEN 'transcribe:open'
          / TRANSCRIBE_REVEAL 'transcribe:reveal'
          / MODELS_LIST 'models:list' / MODELS_DOWNLOAD 'models:download'
          / MODELS_CANCEL 'models:cancel' / MODELS_DELETE 'models:delete' / MODELS_PROGRESS 'models:progress'

type TranscribeReason = 'invalid_argument'|'model_missing'|'audio_not_found'|'whisper_failed'|'cancelled'|'disk_full'|'internal'
type ModelReason = 'invalid_argument'|'download_failed'|'sha_mismatch'|'cancelled'|'disk_full'|'internal'

interface TranscribeApi { start(audioPath,{model,language})→Result<{jobId}>; cancel(jobId)→Result;
  saveAs(md,defaultName)→Result<{path}|null>; openFile(path)→Result; revealInFolder(path)→Result;
  onProgress(cb:{jobId,percent})→()=>void; onSegment(cb:{jobId,startMs,text})→()=>void }
interface ModelsApi { list()→Result<Array<{name,sizeBytes,downloaded}>>; download(name)→Result<{jobId}>;
  cancel(jobId)→Result; delete(name)→Result; onProgress(cb:{name,percent})→()=>void }
ScrubberApi += transcribe: TranscribeApi; models: ModelsApi
```

buildTranscribeArgs (зеркало ffmpeg-args.ts, 03-RESEARCH.md:376-401):
```
interface TranscribeOpts { modelPath; audioPath; language; vadModelPath: string|null; threads }
buildTranscribeArgs(o) → ['-m',modelPath,'-l',language,'-pp','-oj','-t',String(threads),'-f',audioPath, (+ '--vad','-vm',vadModelPath if vadModelPath)]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Расширить shared/ipc.ts контрактом transcribe.* / models.*</name>
  <read_first>
    - src/shared/ipc.ts (весь файл, 130 строк — эталон MediaApi/MediaReason/MediaProgressEvent/Channels/ScrubberApi для зеркалирования)
    - 03-RESEARCH.md §"IPC Contract Additions" (строки 420-451 — точные сигнатуры)
    - 03-PATTERNS.md §"src/shared/ipc.ts" (строки 313-317)
  </read_first>
  <behavior>
    - Channels содержит все 7 TRANSCRIBE_* и 5 MODELS_* строковых констант (значения вида 'transcribe:start', 'models:list')
    - TranscribeReason — union ровно из 7 строк (invalid_argument, model_missing, audio_not_found, whisper_failed, cancelled, disk_full, internal)
    - ModelReason — union ровно из 6 строк (invalid_argument, download_failed, sha_mismatch, cancelled, disk_full, internal)
    - ScrubberApi имеет поля transcribe: TranscribeApi и models: ModelsApi
    - typecheck проходит (npm run typecheck) — контракт компилируется
  </behavior>
  <action>
    Расширить (НЕ переписать) src/shared/ipc.ts по образцу MEDIA_*. Добавить в объект Channels 7 каналов TRANSCRIBE_START/CANCEL/PROGRESS/SEGMENT/SAVE_AS/OPEN/REVEAL и 5 каналов MODELS_LIST/DOWNLOAD/CANCEL/DELETE/PROGRESS со строковыми значениями из interfaces-блока — строковые литералы каналов ТОЛЬКО здесь. Добавить TranscribeReason и ModelReason union-типы. Добавить event-payload интерфейсы TranscribeProgressEvent { jobId; percent } и TranscribeSegmentEvent { jobId; startMs; text } и ModelProgressEvent { name; percent } (зеркало MediaProgressEvent). Добавить TranscribeApi и ModelsApi интерфейсы (точные сигнатуры из interfaces-блока; onProgress/onSegment возвращают unsubscribe () => void). Расширить ScrubberApi полями transcribe: TranscribeApi и models: ModelsApi. Каждое поле снабдить JSDoc со ссылкой на D-XX как в существующих MEDIA_* комментариях.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` проходит без ошибок
    - grep подтверждает наличие `transcribe: TranscribeApi` и `models: ModelsApi` в ScrubberApi
    - grep подтверждает 7 TRANSCRIBE_ и 5 MODELS_ ключей в Channels
    - TranscribeReason содержит 'model_missing' и 'whisper_failed'
  </acceptance_criteria>
  <done>shared/ipc.ts расширен полным контрактом transcribe.*/models.*, typecheck зелёный, все downstream-слайсы могут импортировать типы.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildTranscribeArgs + whisper-paths + RED тест-стабы (Wave 0 Nyquist)</name>
  <read_first>
    - src/main/utilities/ffmpeg-args.ts (37 строк — эталон pure-fn без импортов electron)
    - src/main/services/ffmpeg-paths.ts (70 строк — эталон resolve + app.asar.unpacked-патч + assertBinaryExists:47-57 + ensureExecutable:63-69)
    - src/main/services/media-extractor.test.ts (эталон ForkMock + overrideUserDataPath + vi.mock ffmpeg-paths — для transcriber.test стаба)
    - electron-builder.yml (asarUnpack уже содержит resources/**)
    - 03-RESEARCH.md §"buildTranscribeArgs" (376-401), §"VAD Parameters" (175-195), §"segment + progress parsing" (403-418), §"Validation Architecture" (476-503)
    - 03-PATTERNS.md §"whisper-args.ts" (183-198), §"whisper-paths.ts" (202-221), §"transcriber.test.ts" (297-308)
  </read_first>
  <behavior>
    - whisper-args: buildTranscribeArgs({language:'ru', vadModelPath:'/x/silero.bin', ...}) → массив содержит '-l','ru','-pp','-oj','--vad','-vm','/x/silero.bin','-f' (TRANS-03)
    - whisper-args: при vadModelPath:null массив НЕ содержит '--vad' и '-vm'
    - whisper-args: '-f' получает audioPath as-is (WAV не ресемплится, Pitfall 7)
    - whisper-runner-parse тесты RED: stderr-строка 'whisper_print_progress_callback: progress =  35%' → percent 35 (cap 99); stdout-строка '[00:00:00.000 --> 00:00:03.480]  текст' → {startMs:0, text:'текст'} (TRANS-04)
    - transcriber.test / model-manager.test / transcript-builder.test / integration RED: ссылаются на ещё-не-существующие модули → падают (Nyquist готов)
  </behavior>
  <action>
    (1) Создать src/main/utilities/whisper-args.ts: экспортировать TranscribeOpts { modelPath; audioPath; language; vadModelPath: string | null; threads } и чистую функцию buildTranscribeArgs (без импортов electron/main-графа, зеркало ffmpeg-args.ts) собирающую -m/-l/-pp/-oj/-t/-f и push '--vad','-vm',vadModelPath при ненулевом vadModelPath. Написать whisper-args.test.ts (GREEN — реализация уже есть) с кейсами из behavior.
    (2) Создать src/main/services/whisper-paths.ts: resolveWhisperCli() (резолв resources/whisper/<platform-arch>/whisper-cli(.exe) от process.resourcesPath в packaged / repo-resources/ в dev + .replace('app.asar','app.asar.unpacked')), resolveModel(name) → join(userData,'models',`ggml-${name}.bin`), resolveVadModel() → join(userData,'models','ggml-silero-v5.1.2.bin'), assertBinaryExists(p,name) (дословно из ffmpeg-paths.ts:47-57), assertModelExists(p) (existsSync → throw с маркером для reason model_missing), ensureExecutable (chmod 0o755, win32 no-op — дословно ffmpeg-paths.ts:63-69).
    (3) Создать RED тест-стабы (импортируют будущие модули, описывают ожидаемое поведение, помечены так чтобы падать до реализации): src/main/utilities/whisper-runner-parse.test.ts (regex SEG/PROG из 03-RESEARCH.md:403-418, TRANS-04), src/main/services/transcriber.test.ts (ForkMock + cancel→cancelled + start не блокирует — зеркало media-extractor.test, TRANS-05/06), src/main/services/model-manager.test.ts (мок fetch + sha256 mismatch→download fail, TRANS-02), src/main/services/transcript-builder.test.ts (frontmatter+H1+тело, тумблер пересборки — TRANS-07/D-01/D-02/D-03), tests/integration/transcribe-real.test.ts (gated по наличию whisper-cli как extract-real, TRANS-01). Каждый стаб с минимум 1 it() и TODO-маркером; допускается it.todo/describe.skip снято когда реализация придёт в Wave 1-3.
    Запрещены fenced code-блоки в реализации сверх pure-fn whisper-args. НЕ реализовывать transcriber/model-manager/transcript-builder здесь — только их тест-стабы.
  </action>
  <verify>
    <automated>npm run test:unit -- src/main/utilities/whisper-args.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - whisper-args.test.ts зелёный; покрывает ru+VAD и null-VAD кейсы
    - whisper-paths.ts экспортирует resolveWhisperCli, resolveModel, resolveVadModel, assertBinaryExists, assertModelExists, ensureExecutable; typecheck зелёный
    - whisper-runner-parse.test.ts существует с SEG/PROG-кейсами и RED (нет ещё парсера) — vitest показывает fail/skip, не «no tests»
    - transcriber.test.ts, model-manager.test.ts, transcript-builder.test.ts, tests/integration/transcribe-real.test.ts существуют как стабы
    - whisper-args.ts не импортирует electron/app/main-граф (grep отсутствие import .* electron)
  </acceptance_criteria>
  <done>buildTranscribeArgs реализован и протестирован (GREEN), whisper-paths готов, все Wave 0 тест-стабы заведены (RED), Nyquist-семплинг включён для последующих волн.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    Подготовка whisper-cli-бинарника (НЕ npm-пакет): автоматизация скачала/проверила релиз v1.8.6 и подготовила структуру resources/whisper/win32-x64/. Требуется ручное подтверждение, что бинарник + ВСЕ DLL на месте и значения манифеста (SHA256/размеры моделей) актуальны (A2/A3, Open Q2 из RESEARCH).
  </what-built>
  <how-to-verify>
    1. Подтвердить, что в resources/whisper/win32-x64/ лежат: whisper-cli.exe + ggml.dll + ggml-base.dll + ggml-cpu.dll + whisper.dll (или libwhisper-1.dll) + openblas.dll (если BLAS-вариант). ВСЕ DLL обязаны быть рядом с .exe (Pitfall 1) иначе spawn упадёт.
    2. Выполнить из папки бинарника: `whisper-cli.exe --help` — увидеть список флагов, включая -m, -l, --vad, -vm, -pp, -oj.
    3. Сверить актуальность манифеста моделей (03-RESEARCH.md:160-173): `curl -sI https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-medium.bin` — подтвердить, что файл доступен; при возможности сверить SHA256 medium/large-v3/silero повторно (upstream мог перезалить, A3). Зафиксировать актуальные SHA/размеры в 03-RESEARCH.md манифесте если изменились.
    4. Подтвердить, что resources/whisper/win32-x64/ закоммичен в git (in-tree, не gitignore).
  </how-to-verify>
  <resume-signal>Напишите "approved" или опишите что отсутствует/расходится (DLL, SHA, размеры)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→main (IPC) | Контракт определяется здесь; реальная валидация — в слайсах 02/03. Контракт фиксирует Result-тип (нет throw через границу) и whitelist-форму model-name |
| dev-машина→repo (resources) | whisper-cli + DLL вносятся в git вручную из официального ggml-org релиза |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | shared/ipc.ts контракт | mitigate | Result<T> на все мутации зафиксирован в типах; никаких throw через IPC (компилятор enforce'ит) |
| T-3-02 | Spoofing/Tampering | whisper-cli бинарник в resources/ | mitigate | Источник — официальный github.com/ggml-org/whisper.cpp релиз v1.8.6; вносится в git, ревью в PR |
| T-3-SC | Tampering | npm/whisper install | accept | Phase 3 НЕ добавляет npm-пакетов (RESEARCH §Package Legitimacy Audit); whisper.cpp — бинарник из официального релиза, slopcheck неприменим |
</threat_model>

<verification>
- npm run typecheck зелёный (контракт компилируется)
- npm run test:unit -- src/main/utilities/whisper-args.test.ts зелёный
- Wave 0 тест-стабы существуют и видны vitest (RED/skip, не «no tests found»)
- Чекпоинт: resources/whisper/win32-x64/ заполнен и закоммичен, манифест сверен
</verification>

<success_criteria>
- shared/ipc.ts расширен полным transcribe.*/models.* контрактом
- buildTranscribeArgs реализован и зелёный (TRANS-03)
- whisper-paths.ts готов (resolve + assert guards)
- Все Wave 0 RED-стабы заведены (TRANS-01/02/04/05/06/07)
- whisper-cli.exe + DLL в resources/, манифест сверен (человеческое подтверждение)
</success_criteria>

<output>
Create `.planning/phases/03-local-transcription-core-value/03-01-SUMMARY.md` when done
</output>
