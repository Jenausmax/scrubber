// Заглушка для Task 1 — полноценная форма реализуется в Task 2 этого плана.
// Нужна только чтобы App.tsx собирался с импортом './routes/Settings'.
export default function Settings(): React.JSX.Element {
  return <div className="p-8">Settings (заполняется в Task 2)</div>
}
